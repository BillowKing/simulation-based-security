\documentclass[12pt,letterpaper,fontset=windows]{ctexart}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm,bm}
\usepackage{algorithm}
\usepackage{algorithmicx}
\usepackage{algpseudocode}
\usepackage{enumitem}
\usepackage{float}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{graphicx}
\usepackage{tocloft}
% 目录点线
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}}
\usepackage{hyperref}
\usepackage{cleveref}

% Chinese fonts are managed by ctexart with fontset=windows.
% If necessary, adjust Chinese fonts via ctex options.

% 加上这句
\setcounter{tocdepth}{2}
% ====================== 自定义定理环境 ======================
\theoremstyle{plain}
\newtheorem{theorem}{定理}[section]
\newtheorem{lemma}[theorem]{引理}
\newtheorem{corollary}[theorem]{推论}
\newtheorem{proposition}[theorem]{命题}

\theoremstyle{definition}
\newtheorem{definition}[theorem]{定义}
\newtheorem{sysmodel}[theorem]{系统模型}
\newtheorem{secmodel}[theorem]{安全模型}
\newtheorem{construction}[theorem]{方案构造}
\newtheorem{example}[theorem]{数值样例}

\theoremstyle{remark}
\newtheorem{remark}[theorem]{备注}
\newtheorem{proofidea}[theorem]{证明思路}
\newtheorem{fullproof}[theorem]{完整安全证明}

% ====================== 密码学专用符号 ======================
\newcommand{\Real}{\mathsf{Real}}
\newcommand{\Ideal}{\mathsf{Ideal}}
\newcommand{\View}{\mathsf{View}}
\newcommand{\Sim}{\mathcal{S}}
\newcommand{\Adv}{\mathcal{A}}
\newcommand{\PPT}{\mathsf{PPT}}
\newcommand{\negl}{\mathsf{negl}}
\newcommand{\eqc}{\overset{c}{\equiv}} % 计算不可区分
\newcommand{\Gen}{\mathsf{Gen}}
\newcommand{\Enc}{\mathsf{Enc}}
\newcommand{\Dec}{\mathsf{Dec}}
\newcommand{\Com}{\mathsf{Com}}
\newcommand{\Open}{\mathsf{Open}}
\newcommand{\Verify}{\mathsf{Verify}}
\newcommand{\Func}{\mathcal{F}}
\newcommand{\ZKPoK}{\mathsf{ZKPoK}}
% \DH is already a built-in TeX command, so use a different macro name for Diffie-Hellman
\newcommand{\DHD}{\mathsf{DH}}
\newcommand{\ddh}{\mathsf{DDH}}

% ====================== 算法伪代码汉化 ======================
\algnewcommand{\Let}{\textbf{let}}
\algrenewcommand{\algorithmicreturn}{\textbf{return}}
\algnewcommand{\Output}{\textbf{output}}
\algnewcommand{\Newline}{\Statex}
\algrenewcommand{\algorithmicrequire}{\textbf{输入：}}
\algrenewcommand{\algorithmicensure}{\textbf{输出：}}

% ====================== 文档信息 ======================
\title{\bfseries 基于模拟的安全性定义和证明}
\author{王涛 \ \ 陕西师范大学 \ \ water@snnu.edu.cn}
\date{2026年6月12日}

\begin{document}
\maketitle
\begin{abstract}
我一直以为对于基于模拟的安全性定义和证明自己懂了一点，现在看，理解的还是不透。为此，我觉得需要彻底解决这个问题，一方面让自己能够真正的理解；另一方面写个东西方便日后学生容易懂。本文力图系统性地讲解密码学中\textbf{基于模拟的安全（Simulation-Based Security）}证明范式，从起源、发展和典型案例等方面展开。特别地，以四个经典密码原语（承诺协议、零知识证明、公钥加密、密钥交换）为例展开完整分析，覆盖\textbf{系统模型定义、基于模拟的形式化安全模型、详细方案构造、安全性证明}四个核心环节。
\end{abstract}
\clearpage
\tableofcontents
\clearpage

% ====================== 正文开始 ======================
\section{引言}
根据可证明安全的思想，将一个密码算法或者协议部署在实际安全系统之前，我们首先需要证明它可以抵御所有的攻击手段。我们先针对实际应用环境中假想的敌手的能力来构建一个敌手模型(adversarial model)。在这个敌手模型中，我们规范了敌手可以做什么、不能做什么。下一步，我们需要证明的是任何敌手在规定的能力约束范围内都无法攻破某个密码算法或者协议。一旦达到这个目的，我们的目标算法或者协议就被称为是可证明安全的\cite{CCCF2019}。

可证明安全的核心思想是“归约”(reduction)：我们需要把错综复杂的密码算法或者协议的安全性问题归约到某个（或者某几个）极微本原（atomic primitives，安全方案或协议的最基本组成构件或模块，俗称原语），那么密码学家只需要分析这几个极微本原的安全性就可以了。

基于游戏的安全性定义是可证明安全中最常用的定义方法之一。它通过构建一个形式化的安全游戏来描述敌手与挑战者之间的交互过程，定义了敌手在该游戏中获胜的条件，并将协议的安全性归约到敌手在该游戏中的成功概率上。这种方法直观且易于理解，适用于许多基本密码原语的安全分析。然而，基于游戏的定义也有自己的局限性。其中最重要的特点是，一般来说它只能模拟协议在孤立环境下单独运行的安全性，无法对复杂并发环境的安全性进行建模。
\begin{figure}[H]
\centering
\includegraphics[width=0.7\textwidth]{fig1.png}
\caption{基于模拟的安全性定义：真实世界与理想世界视图比较\cite{CCCF2019}}
\label{fig:sim-security}
\end{figure}
基于模拟的安全性定义（Simulation-based Security）是现代密码学中用于形式化描述协议安全性的一种根本性范式。如图\ref{fig:sim-security} 所示，其核心思想是：协议参与者$(P_1, P_2, P_3)$、攻击者 ($\Adv$)、仿真攻击者 ($\Sim$) 和环境机 ($\mathcal{Z}$)都是由概率多项式时间(Probabilistic Polynomial Time, PPT)交互通用图灵机(ITM) 来模拟。设计者把对于目标密码协议的功能性要求和安全性要求都通过定义$\mathcal{F}$来模拟。该密码协议的安全性强弱完全取决于$\mathcal{F}$的具体定义。环境机 ($\mathcal{Z}$)的目的是区分现实世界和理想世界，在运行过程中它为协议参与者提供协议输入，并可以读取协议输出；同时，环境机可以实时和攻击者通信。如果对于任何攻击者通过攻击现实世界中的协议所产生的影响都可以被仿真攻击者在理想世界中模拟，那么环境机将无法区分现实/理想世界。我们就认为该密码协议的安全性至少和在理想世界中用$\mathcal{F}$计算一样安全\cite{CCCF2019}。
\subsection{“Simulation-based”概念起源与发展}
模拟思想与零知识证明的发展密切相关。早期关于零知识（zero-knowledge）的工作由 Goldwasser、Micali 与 Rackoff(会议版本为1985年)等人奠定，证明了可以在不泄露证据的前提下进行证明\cite{Goldwasser1989}。随后，Goldreich、Micali 和 Wigderson（GMW, 1987）等人的工作把“理想/真实世界”式的模拟思想引入到安全多方计算（MPC）与协议安全的形式化定义中，明确了通过构造模拟器来证明协议不泄露额外信息的范式\cite{Goldreich1987}。Beaver、Micali、Rogaway明确使用 Trusted Party（可信第三方）作为理想世界,更系统地刻画理想执行与真实执行之间的关系\cite{Beaver1990}。

随着研究的深入，模拟范式被不断强化与扩展。最重要的里程碑之一是2001年Ran Canetti提出的通用可组合性（Universal Composability, UC）框架\cite{Canetti2001}，它把理想/真实的模拟思想提升为一个可组合的证明体系，使得在任意上下文中安全保持不变成为可能。UC 框架引入了更强的理想功能与环境模型，并指出了在没有适当设置（如公共参考字符串、证书注册局或可信设定）下的某些不可能性结果。

与基于游戏的定义（例如 IND-CPA、EUF-CMA 等）相比，基于模拟的定义具有以下显著特点：
\begin{itemize}
  \item 全局语义保证：模拟定义直接比较敌手所见的全部视图，能够一次性捕捉多轮交互的复合泄漏，适合需要强语义保证的高阶原语与复合协议。
  \item 可组合性（在 UC 或其变体下）：若协议在可组合框架下被证明安全，则可安全地作为子协议嵌入更大系统。
  \item 证明成本与假设：模拟式证明往往需要构造强模拟器（有时是回溯式或依赖黑盒/白盒策略），证明复杂度和构造难度通常高于基于游戏的归约。
\end{itemize}

尽管强大，模拟范式也存在实际与理论上的限制：
\begin{itemize}
  \item 强定义带来的不可能性：在没有额外设置的情况下，某些理想化安全目标不可实现，导致必须引入可信初始化（setup）或对功能进行弱化；
  \item 模拟器构造难度：对于某些协议，存在证明上的技术障碍（例如需要重置或回溯敌手），构造高效模拟器并非总是可行；
  \item 性能与工程折中：为获得可组合性或更强的模拟性质，通常需要更多的密码学假设或更重的运行开销。
\end{itemize}

\subsection{内容安排}
基于上述背景，本文的目标是系统讲解基于模拟的安全范式。具体目标包括：
\begin{itemize}
  \item 阐明理想/真实世界的核心概念与直觉，并通过实例示范如何构造模拟器与进行不可区分性分析；
  \item 用四类经典原语（承诺、零知识、公开密钥加密、密钥交换、Hybrid）作为切入点，逐一给出系统模型、理想功能、方案构造与基于模拟的安全证明；
  \item 讨论可组合性扩展（如 UC）与常见实际部署时的设置假设，帮助读者理解理论证明与工程实现之间的桥梁。
\end{itemize}

接下来的章节将按从基础到进阶的顺序展开：第~2 节给出模拟安全范式的核心逻辑与常用工具，第~3 节从承诺协议开始给出完整实例并证明安全性，随后分别讨论零知识、公钥加密与密钥交换等原语，最后讨论可组合性扩展与若干前沿问题。

\section{基础知识}
首先给出一些基础知识，包括密码学安全性证明的基本概念、基于游戏的安全性定义与证明方法，以及基于模拟的安全性定义的核心逻辑与等价命题。这些内容为后续章节中具体协议的安全证明提供必要的理论工具和分析框架。
\subsection{密码学安全性证明的基本概念}
密码学安全性证明是为密码方案提供形式化保证的核心工具，它不依赖于直觉性的描述，而是用严格的数学语言定义“安全”的含义。

一个完整的安全证明通常包含以下关键要素：
\begin{itemize}
  \item \textbf{安全参数} $\kappa$：用于量化算法的输入规模和安全强度，随着 $\kappa$ 增大，安全性应当提升。
  \item \textbf{敌手模型}：明确敌手的能力边界，例如是否能控制网络、阅读或篡改消息、收买参与方等。在定义中刻画敌手模型就是建模敌手的能力。
  \item \textbf{安全目标}：采用可验证的数学性质描述方案应满足的安全属性，如机密性、完整性、认证性或零知识性。
  \item \textbf{复杂度假设}：将协议的安全性归约到公认的困难问题或者已证明的安全性上，例如离散对数、DDH、RSA 或签名不可伪造性等。
\end{itemize}

在密码学中，安全性的衡量不仅仅是“敌手能否成功”，而是要求敌手的成功概率随安全参数增长而衰减为\textbf{可忽略函数}。具体来说，函数 $\negl(\kappa)$ 当且仅当对任意多项式 $p(\kappa)$，存在足够大的 $\kappa$ 使得
\[ \negl(\kappa) < \frac{1}{p(\kappa)}. \]
这种定义保证了攻击成功概率在实际可计算范围之外。

正式证明通常采用“游戏”或“模拟”的方式，将协议执行过程抽象为一种交互过程，并严格说明敌手与协议参与方之间的信息流与步骤。
\subsection{基于游戏的安全性证明及实例}
基于游戏的安全性证明将安全属性描述为敌手与一个挑战者之间的交互游戏。敌手的成功概率被定义为其在游戏中获胜的概率，安全性则要求该成功概率在合理假设下可忽略。
\subsection{安全模型实例：IND-CPA}
一个经典的加密安全性目标是\textbf{选择明文攻击下的不可区分性}（IND-CPA）。其游戏定义如下：
\begin{enumerate}
  \item 初始化阶段：挑战者生成密钥 $k\gets\mathcal{K}$，将公钥或密钥信息保留，向敌手$\Adv$公开必要的公共参数。
  \item 查询阶段：敌手 $\Adv$ 可多次提交明文查询 $m$，挑战者返回相应的密文 $c=\Enc_k(m)$。
  \item 挑战阶段：敌手提交两个等长明文 $(m_0,m_1)$，挑战者随机选取比特 $b\gets\{0,1\}$，返回挑战密文 $c^*=\Enc_k(m_b)$。
  \item 猜测阶段：敌手输出猜测 $b'$，若 $b'=b$ 则敌手获胜。
\end{enumerate}

敌手相对于该游戏的优势定义为：
\[
\mathsf{Adv}_{\Adv}^{\mathsf{IND\text{-}CPA}}(\kappa) = \left|\Pr[b'=b] - \tfrac12\right|.
\]

\begin{definition}[IND-CPA安全性]
一个加密方案$\mathcal{E}$满足IND-CPA安全性，如果对任意PPT敌手$\Adv$，在上述游戏中获胜的优势为可忽略函数：
\[
\mathsf{Adv}_{\Adv}^{\mathsf{IND\text{-}CPA}}(\kappa) \le \negl(\kappa)
\]
\end{definition}

\subsubsection{实例：基于伪随机函数的随机化对称加密}
首先需要确定的是不存在加密算法为确定性的CPA安全加密方案（有状态加密方案除外）。因为对于随机选取的明文 $m,m' \in \mathcal{M}_\lambda$，攻击者总能轻易区分密文对 $\bigl(\Enc(k, m), \Enc(k, m)\bigr)$ 和 $\bigl(\Enc(k, m), \Enc(k, m')\bigr)$。事实上，\textbf{所有满足CPA安全的加密方案，其加密算法都是随机化的}。

考虑如下随机化对称加密方案 $\mathcal{E}=(\Gen,\Enc,\Dec)$：
\begin{itemize}
  \item $\Gen(1^\kappa)$：随机生成密钥 $k\gets\{0,1\}^\kappa$。
  \item $\Enc_k(m)$：随机选择 $r\gets\{0,1\}^\kappa$，输出密文
  \[ c = (r,\; F_k(r) \oplus m), \]
  其中 $F_k$ 是带密钥的伪随机函数，定义为$F_k: \{0,1\}^k \times \{0,1\}^k \to \{0,1\}$。
  \item $\Dec_k((r,s))$：计算 $m = F_k(r) \oplus s$ 并返回明文。
\end{itemize}

该方案使用随机块 $r$ 作为一次性随机化向量，保证相同明文每次加密结果不同，从而避免重复明文泄露。此时，若 $F_k$ 是安全的伪随机函数，则方案可以满足IND-CPA安全。


我们下面证明若存在对上述方案具有非忽略优势的IND-CPA敌手 $\Adv$，则可以构造一个伪随机函数区分器 $\mathcal{D}$，攻破 $F$ 的安全性。

\begin{theorem}
如果 $F$ 是一个安全的伪随机函数，则上述加密方案 $\mathcal{E}$ 满足IND-CPA安全性。
\end{theorem}
\begin{proof}

构造区分器 $\mathcal{D}$：
\begin{enumerate}
  \item $\mathcal{D}$ 从伪随机函数挑战中获得一个查询谕言机 $\mathcal{O}(\cdot)$，它要判断该接口是一个真实的伪随机函数 $F_k$ 还是一个真正的随机函数。
  \item $\mathcal{D}$ 运行敌手 $\Adv$，并在 $\Adv$ 的任何密文查询 $m$ 时：
    \begin{itemize}
      \item 随机选择 $r\gets\{0,1\}^\kappa$；
      \item 令 $s = \mathcal{O}(r) \oplus m$；
      \item 返回密文 $(r,s)$ 给 $\Adv$。
    \end{itemize}
  \item 当 $\Adv$ 提出挑战对 $(m_0,m_1)$ 时，$\mathcal{D}$ 依然随机选择 $r^*\gets\{0,1\}^\kappa$，构造
    \[ c^* = \bigl(r^*, \; \mathcal{O}(r^*) \oplus m_b\bigr) 
    \]
    并将其交给 $\Adv$。
  \item 最终，$\mathcal{D}$ 输出 $\Adv$ 的猜测 $b'$ 作为自身的判别结果。
\end{enumerate}

分析：
\begin{itemize}
  \item 若 $\mathcal{O}(\cdot)$ 是真实的伪随机函数 $F_k(\cdot)$，则 $\mathcal{D}$ 为 $\Adv$ 提供的密文分布与真实加密方案完全一致，故
  \[ \Pr[\mathcal{D}\text{ 输出 }1 \mid \mathcal{O}=F_k] = \Pr[b'=b]. \]
  \item 若 $\mathcal{O}(\cdot)$ 是真正的随机函数，则对每个用于加密的随机 $r$，值 $\mathcal{O}(r)$ 是独立且均匀的二进制串，因此挑战密文中的掩码部分
  \[ \mathcal{O}(r^*) \oplus m_b 
  \]
  对 $b$ 来说是信息上不可区分的。此时
  \[ \Pr[\mathcal{D}\text{ 输出 }1 \mid \mathcal{O}\text{ 随机}] = \tfrac12. \]
\end{itemize}

因此，$\mathcal{D}$ 的区分优势满足：
\[
\mathsf{Adv}_{\mathcal{D}}^{\mathsf{PRF}}(\kappa) = \left|\Pr[b'=b] - \tfrac12\right| = \mathsf{Adv}_{\Adv}^{\mathsf{IND\text{-}CPA}}(\kappa).
\]
如果 $\Adv$ 的IND-CPA优势为不可忽略的函数，则 $\mathcal{D}$ 对PRF的区分优势也为不可忽略的函数，这将攻破$F$的伪随机安全性。于是该加密方案在伪随机函数假设下满足IND-CPA安全。
\end{proof}

通过这个实例我们可以看到，基于游戏的安全性证明既能给出明确的攻击模型，又能够将具体方案的安全性归约到更基本的密码学假设上。

\section{基于模拟的安全性定义}
\subsection{真实世界 vs 理想世界}
基于模拟的安全的本质是\textbf{用理想世界的“天然安全”，反证真实世界的“协议安全”}。两个世界的严格定义对比如下：

\begin{table}[H]
\caption{真实世界与理想世界的严格对比}
\centering
\begin{tabularx}{\textwidth}{l|X|X}
\toprule
\textbf{维度} & \textbf{真实世界（Real World）} & \textbf{理想世界（Ideal World）} \\
\midrule
参与方 & 诚实协议执行方 + \textbf{恶意敌手 $\Adv$} & 诚实协议执行方 + \textbf{模拟器 $\Sim$} + 可信第三方（TTP） \\
通信环境 & 公开信道，敌手\textbf{完全控制网络} & 理想安全信道，仅通过TTP交互 \\
敌手行为 & 主动攻击：监听、篡改、丢弃消息、收买参与方 & \textbf{无恶意敌手}，$\Sim$仅负责复刻敌手视图 \\
安全逻辑 & 协议本身对抗攻击 & TTP天然保证安全，$\Sim$模拟“攻击效果” \\
\bottomrule
\end{tabularx}
\end{table}

\begin{remark}
模拟器 $\Sim$ \textbf{不是敌手}：它不进行任何实际攻击，仅依靠公开信息、不掌握诚实方私密输入，生成与真实世界敌手看到的内容完全不可区分的伪视图。
\end{remark}

\subsubsection{真实协议和其执行过程}
遵循Dan Boneh的思路，我们通过一个MPC协议来描述真实世界和模拟世界的定义和逻辑\cite{boneh_graduate_2023}。
首先，我们详细地描述多方（$N$方）协议及其执行过程。系统中有$N$个参与方$P_1,\dots,P_N$，每个参与方独立运行协议$\Pi$。我们假定每个参与方$P_i$知晓自身的编号$i$。系统中存在敌手$\mathcal{A}$(\textit{当前假定其为恶意敌手})。

若$P_1,\dots,P_N$中存在被收买的参与方，我们直接将被收买方的行为纳入敌手$\mathcal{A}$的行为逻辑中。该建模方式刻画了如下现实：被收买的参与方之间可以相互串通，也能与外部攻击者勾结。因此将所有腐化行为统一抽象为单一实体$\mathcal{A}$会更加简便。

我们假定所有参与方通过异步通信信道$\mathcal{C}$交互。一般假设$\mathcal{C}$是安全的点对点信道，每条点对点信道同时保障消息的隐私性与完整性。我们将底层实现全部抽象，直接把$\mathcal{C}$建模为具备物理安全性的点对点信道，即便信道本身安全，敌手仍能通过信道传输的消息获取部分信息、并对消息施加一定控制，具体规则如下：
\begin{itemize}
    \item 消息发送时，敌手能够获知消息发送事件、消息长度、发送方身份以及目标接收方。该设定刻画了两类现实特性：\textit{一是}加密方案会泄露密文长度相关信息；\textit{二是}在真实网络部署中，敌手能够观测消息的发送者与接收者。
    \item 消息的送达时机与是否送达由敌手决定。该设定对应异步通信信道$\mathcal{C}$的特性：信道不保证消息一定送达、也不保证送达时延，因此我们将消息调度权交由敌手掌控。
    \item 敌手无法获取消息的其余信息，也不能篡改消息、伪造消息发送者身份，更不能将消息转发给目标接收方以外的其他参与方。
\end{itemize}

整个模型中还有一个核心组件，我们称之为\textbf{环境}。环境负责向诚实参与方提供输入、接收诚实参与方输出；同时由环境判定哪些参与方被收买、哪些保持诚实。协议执行过程中，环境还可与敌手$\mathcal{A}$协同交互。我们用符号$\mathcal{Z}$表示环境。

图\ref{fig2}所示为上述MPC协议的真实执行和其基本拓扑：

\begin{figure}[H]
\centering
\includegraphics[width=1.0\textwidth]{fig2.pdf}
\caption{UC框架下的MPC协议真实世界vs模拟世界\cite{boneh_graduate_2023}}
\label{fig2}
\end{figure}
\begin{itemize}
\item 在图中，有四个参与方 $P_1, P_2, P_3, P_4$，但假定$P_3$和$P_4$被收买，并在图中“吸收”到了敌手$\Adv$中。
\item 通信网络 $\mathcal{C}$ 包含4条线路，原本分别对接4个参与方；但由于参与方 $P_3$、$P_4$ 已包含在敌手 $\mathcal{A}$，他们的线路将直接连通至 $\mathcal{A}$。基于此设定，敌手 $\mathcal{A}$ 若想向 $P_1$ 发送一条看似来自 $P_3$ 的消息，只需通过标号为3的线路传输该消息即可。同理，$P_1$ 发给 $P_3$ 的消息也会沿这条线路抵达 $\mathcal{A}$。我们将这类线路称为 $\mathcal{C}$-I/O线路（$\mathcal{C}$-I/O wires）。
\item 敌手 $\mathcal{A}$ 与网络 $\mathcal{C}$ 之间还存在一条控制线路，我们称之为 $\mathcal{C}$-控制线。敌手 $\mathcal{A}$ 可通过这条控制线接收来自网络 $\mathcal{C}$ 的“泄露消息”，这类消息会向敌手泄露诚实参与方所发送协议消息的部分信息。同时，敌手 $\mathcal{A}$ 也能经由该控制线向网络 $\mathcal{C}$ 发送“控制消息”，以此指令网络 $\mathcal{C}$ 何时向诚实参与方交付协议消息。
\item 模型中还包含环境机 $\mathcal{Z}$，环境机通过线路与诚实参与方 $P_1$、$P_2$ 相连，可经由这些线路输入协议的输入、接收协议的输出，我们将这类线路称作 $\mathcal{Z}$-I/O线路。环境机 $\mathcal{Z}$ 与敌手 $\mathcal{A}$ 之间同样设有一条控制线路，称为 $\mathcal{A}$-控制线，二者可通过该线路直接通信。其核心逻辑为：环境机 $\mathcal{Z}$ 可向敌手 $\mathcal{A}$ 发送“控制消息”，用以指导敌手后续的行为；同时环境控制线路也能接收来自敌手的“泄露消息”，该消息代表泄露至环境控制线路的各类信息。
\end{itemize}
\begin{remark}[环境机的角色]
顾名思义，环境机用于对协议执行所处的外部环境进行建模。实际场景中，某一协议可作为子协议嵌入更大的上层协议，而上层协议的所有其余组件都会被抽象封装，统一由$\mathcal{Z}$这一实体建模。
由于允许环境在协议执行过程中与敌手交互，我们便能够刻画子协议与上层协议之间所有可能发生的交互行为。基于这一设定，我们可以证明关键的\textit{组合定理}(composition theorems)，其直观含义为：若一个协议本身是安全的，那么无论将它作为子协议嵌入任意上层协议，或是并发运行该协议的多个实例，它都能维持安全性。
正因这些组合定理成立，图\ref{fig2}所介绍的安全模型被称为\textbf{通用可组合框架}。
\end{remark}
\textit{真实协议执行要点}：
\begin{itemize}
\item 由于模拟范式的核心思想是将协议的参与方建模为交互式图灵机（Interactive Turing Machines, ITM），因此在协议的执行中，用机器来代表包括环境、敌手在内的各个参与方。相应地，网络的通信被抽象为机器之间的消息传递。图\ref{fig2}中刻画的真实协议执行模型隐含着多机器之间的并发。意思是说，实际上敌手可以掌控各台机器计算操作的交错调度顺序。
\item  环境机$\mathcal{Z}$开始执行协议后，确定了哪些参与方被收买，这些信息被写入仅由$\mathcal{C}$和$\mathcal{A}$可见的特殊磁带（tape），其对诚实参与方不可见，这里刻画的事实是：诚实参与方不知道哪些参与方被收买了。
\item 敌手和诚实参与方之间不能直接交互，所有的交互都要经过安全信道间接完成。如果考虑非安全信道，那么需要定义额外的敌手来建模。
\item 定义环境$\mathcal{Z}$是\textit{行为良好的}(well behaved)，当且仅当其运行时间可由安全参数的某个多项式严格上界约束（该多项式可与环境$\mathcal{Z}$自身相关）。
\item 称敌手$\mathcal{A}$与协议$\Pi$\textit{相容}(compatible with)\footnote{“相容”、“兼容”、“可容许的”等等翻译缺乏统一的说法，这个问题后面专门讨论。}，当且仅当对任意行为良好的环境$\mathcal{Z}$，所有诚实参与方$P_i$与敌手$\mathcal{A}$的总运行时间，以\textbf{压倒性概率}被某个多项式约束；该多项式由协议$\Pi$与敌手$\mathcal{A}$决定，与环境$\mathcal{Z}$无关，多项式的自变量包含安全参数、以及环境经由$\mathcal{Z}$-I/O线路、$\mathcal{A}$-控制线所发送全部消息的总长度。
\item 基于上述定义，若$\mathcal{Z}$为行为良好的，且敌手$\mathcal{A}$与协议$\Pi$相容，则所有机器的整体运行时间将以压倒性概率被某个多项式约束；该多项式由协议$\Pi$、敌手$\mathcal{A}$、环境$\mathcal{Z}$三者共同决定，自变量为安全参数。
\item 若平凡敌手与协议$\Pi$相容，则称协议$\Pi$是\textbf{高效协议}。
\end{itemize}

\subsubsection{理想世界和其执行过程}
如图\ref{fig2}所示，相对于真实世界，理想世界定义的变化是：
\begin{itemize}
\item 理想世界存在一个不同的敌手$\Sim$，称为\textit{模拟器}；
\item 定义一个\textit{理想功能}（或称为函数，ideal functionality）$\mathcal{F}$，由一个绝对可信的第三方（Trusted Party, TTP）实现。$\mathcal{F}$ 定义了协议安全合规的理想行为，其具体内容由协议的目标决定。例如，在一个多方安全函数计算的MPC中，$\mathcal{F}$ 的功能就是：接收各方输入，计算目标函数，并将结果返回给各方。在一个零知识证明中，$\mathcal{F}$ 的功能就是：接收证明者的证据和验证者的挑战，验证证据的有效性，并返回验证结果。
\item 可以看出，诚实参与方在图中消失了，因为他们的从环境机$\mathcal{Z}$收到的输入消息直接转交给$\mathcal{F}$ ，而$\mathcal{F}$ 的输出直接返回给环境机$\mathcal{Z}$。因此，理想世界中不存在任何诚实参与方的计算过程。
\end{itemize}
\textit{理想协议执行要点}：
\begin{itemize}

\item  环境机$\mathcal{Z}$开始执行协议后，确定了哪些参与方被收买，这些信息被写入仅由$\mathcal{C}$和$\mathcal{A}$可见的特殊磁带（tape），其对诚实参与方不可见，这里刻画的事实是：诚实参与方不知道哪些参与方被收买了。
\item 敌手和诚实参与方之间不能直接交互，所有的交互都要经过安全信道间接完成。如果考虑非安全信道，那么需要定义额外的敌手来建模。
\item 定义环境$\mathcal{Z}$是\textit{行为良好的}(well behaved)，当且仅当其运行时间可由安全参数的某个多项式严格上界约束（该多项式可与环境$\mathcal{Z}$自身相关）。
\item 称敌手$\mathcal{A}$与协议$\Pi$\textit{相容}(compatible with)\footnote{“相容”、“兼容”、“可容许的”等等翻译缺乏统一的说法，这个问题后面专门讨论。}，当且仅当对任意行为良好的环境$\mathcal{Z}$，所有诚实参与方$P_i$与敌手$\mathcal{A}$的总运行时间，以\textbf{压倒性概率}被某个多项式约束；该多项式由协议$\Pi$与敌手$\mathcal{A}$决定，与环境$\mathcal{Z}$无关，多项式的自变量包含安全参数、以及环境经由$\mathcal{Z}$-I/O线路、$\mathcal{A}$-控制线所发送全部消息的总长度。
\item 基于上述定义，若$\mathcal{Z}$为行为良好的，且敌手$\mathcal{A}$与协议$\Pi$相容，则所有机器的整体运行时间将以压倒性概率被某个多项式约束；该多项式由协议$\Pi$、敌手$\mathcal{A}$、环境$\mathcal{Z}$三者共同决定，自变量为安全参数。
\item 若平凡敌手与协议$\Pi$相容，则称协议$\Pi$是\textbf{高效协议}。
\end{itemize}

\subsection{基于模拟的安全性定义}
基于模拟的安全的统一判定条件为：
\begin{definition}[基于模拟的安全]
  \label{def:sim-security}
协议 $\Pi$ 密码学上安全实现理想功能 $\Func$（理想功能由TTP实现，定义原语的安全合规行为），当且仅当对任意\textbf{概率多项式时间（PPT）真实世界敌手 $\Adv$}，必然存在一个\textbf{PPT模拟器 $\Sim$}，使得两个随机变量分布计算不可区分：
\[
\View_\Adv^\Real(\Pi) \eqc \View_\Sim^\Ideal(\Func)
\]
其中 $\View_\Adv^\Real$ 表示敌手在真实协议执行中的视图，$\View_\Sim^\Ideal$ 表示模拟器在理想世界中生成的伪视图。
\end{definition}

\subsubsection{定义\ref{def:sim-security}的深层内涵：视图不可区分与信息零泄露的等价逻辑}
公式各符号与双世界角色的释义：
\begin{enumerate}
\item $\View_{\Adv}^\Real(\Pi)$：真实世界敌手视图。
    真实世界不存在可信第三方 $\mathsf{TTP}$，协议 $\Pi$ 在完全敌对的公开信道执行；$\Adv$ 是主动PPT恶意敌手，可完成消息监听、篡改、重放、参与方自适应收买等全部攻击行为。视图包含敌手观测到的全部信息：全局公开参数、所有交互报文、被收买参与方的私有输入、各方算法随机硬币、完整会话执行日志，代表敌手从真实协议中获取的全部潜在信息。
\item $\View_{\Sim}^\Ideal(\mathcal{F})$：理想世界模拟器视图。
    理想世界内置绝对可信第三方 $\mathsf{TTP}$，由 $\mathsf{TTP}$ 抽象实现理想功能 $\mathcal{F}$（$\mathcal{F}$ 剥离协议具体实现，仅定义协议安全合规的理想行为，天然不存在安全漏洞）。\textbf{理想世界不存在任何恶意敌手}，敌手的观测位置被PPT模拟器 $\Sim$ 替代；模拟器存在强约束：生成视图全程不访问任何诚实参与方的私密输入、私钥、私有证据，仅能使用全局公开参数与独立均匀随机数。
\item $\overset{c}{\equiv}$：计算不可区分。
    对任意PPT概率区分器 $\mathcal{D}$，区分两类视图的优势满足：
    \[
    \big|\Pr\left[\mathcal{D}\left(\View_{\Adv}^\Real\right)=1\right] - \Pr\left[\mathcal{D}\left(\View_{\Sim}^\Ideal\right)=1\right]\big| \le \negl(\kappa)
    \]
    即在多项式时间内，不存在有效算法分辨视图来源于真实协议还是无私密知识的模拟器。
\end{enumerate}

理想/真实世界敌手存在规则：
\begin{enumerate}
\item 真实世界（左侧视图对应环境）：\textbf{存在恶意PPT敌手 $\Adv$}，敌手以窃取隐私、破坏协议规则为目标，拥有完整网络控制权；
\item 理想世界（右侧视图对应环境）：\textbf{无任何恶意敌手}，仅存在无攻击能力、无私密知识的模拟器 $\Sim$，模拟器仅负责复刻敌手观测痕迹，不实施任何攻击行为。
\end{enumerate}

视图不可区分与「不泄漏私密信息」的逻辑链：
安全定义的本质等价命题：\textit{协议满足基于模拟的安全，当且仅当PPT敌手无法从协议交互中获取任何可利用的私密信息}。完整推导如下：
\begin{enumerate}
\item 模拟器前提约束：$\Sim$ 生成模拟视图时，完全不掌握任何诚实方私密数据（承诺消息、零知识证据、解密私钥、DH临时私钥、签名私钥等），仅依靠公开参数即可生成完整交互日志；
\item 反证假设：若协议存在可被PPT敌手检测的信息泄漏，即敌手可从 $\View_{\Adv}^\Real$ 中提取仅私密输入可推导的隐私值 $X$；
\item 构造区分器：设计PPT区分器 $\mathcal{D}$，输入视图后尝试提取隐私 $X$；若成功提取则判定为真实视图，否则判定为模拟器视图；
\item 矛盾导出：该区分器拥有不可忽略的区分优势，与 $\View_{\Adv}^\Real \overset{c}{\equiv} \View_{\Sim}^\Ideal$ 的定义冲突；
\item 结论：假设不成立，满足该模拟安全定义的协议不存在任何可被PPT敌手利用的隐私泄漏，实现语义层面全局零信息泄露。
\end{enumerate}

与游戏式安全范式的泄漏刻画能力对比：
\begin{itemize}
\item 基于游戏的安全（IND-CPA、EUF-CMA等）：仅能刻画单一维度的局部信息不泄露，无法覆盖全流程隐性、统计侧信道泄漏，多安全属性需要独立证明，无法保证组合后无泄漏；
\item 基于模拟的安全（本定义）：视图包含敌手全程全部观测数据，一次性覆盖协议所有交互阶段，只要视图整体可被无私密知识模拟器复刻，即可保证协议全流程不存在任何形式的隐私泄漏，是全局完备的语义安全标准。
\end{itemize}

几个经典原语的实例对应：
\begin{enumerate}
\item Pedersen承诺：模拟隐藏性等价于模拟器无需明文 $m$ 即可生成合法承诺，恶意接收方无法从承诺值获取消息任意比特，消息零泄漏；
\item GMR图同构零知识证明：零知识性等价于模拟器无需私密同构置换 $\pi$ 即可生成全套交互记录，恶意验证方全程无法获取证据，证据零泄漏；
\item ElGamal公钥加密：模拟IND-CPA安全等价于模拟器无需明文与解密私钥即可生成合法密文，敌手无法从密文反推明文，明文零泄漏；
\item Sigma认证密钥交换：模拟会话密钥安全等价于模拟器无需DH私钥、签名私钥即可生成完整协商报文，中间人敌手无法区分真实会话密钥与均匀随机串，会话密钥零泄漏。
\end{enumerate}

补充注解：
\begin{enumerate}
\item 完美不可区分 $\overset{p}{\equiv}$：视图概率分布完全重合，敌手无论计算能力强弱均无法提取信息，对应信息论安全（如Pedersen承诺隐藏属性）；
\item 计算不可区分 $\overset{c}{\equiv}$：仅约束PPT多项式时间敌手，适用于绝大多数基于离散对数、DDH假设的计算安全方案；
\item 该定义同时屏蔽两类泄漏：直接明文泄漏、多轮交互产生的统计相关性间接侧信道泄漏。
\end{enumerate}

\begin{definition}[敌手视图]
敌手视图 $\View_\Adv$ 包含协议执行过程中敌手能获取的所有信息：
\begin{itemize}
\item 所有公开参数、公开传输的消息；
\item 被敌手收买的参与方的内部状态、私密输入；
\item 敌手自身的随机操作种子；
\item 协议执行过程中所有的中间交互结果。
\end{itemize}
\end{definition}

\subsection{计算不可区分}
\begin{definition}[计算不可区分]
两个参数化随机变量分布族 $\{X_\kappa\}_{\kappa\in\mathbb{N}}$、$\{Y_\kappa\}_{\kappa\in\mathbb{N}}$（$\kappa$ 为安全参数）计算不可区分，当且仅当对任意PPT概率区分器 $\mathcal{D}$，区分优势可忽略：
\[
\left|\Pr\left[\mathcal{D}(X_\kappa)=1\right] - \Pr\left[\mathcal{D}(Y_\kappa)=1\right]\right| \le \negl(\kappa)
\]
其中 $\negl(\kappa)$ 是比任意多项式函数衰减更快的无穷小量。
\end{definition}

\subsection{模拟器的核心类型}
不同协议采用不同的模拟策略，常见两类模拟器在证明逻辑上有本质区别：
\begin{itemize}
\item \textbf{黑盒模拟器}：仅将敌手 $\Adv$ 作为可调用的黑盒算法，不依赖 $\Adv$ 的具体代码、内部逻辑，仅通过输入输出交互完成模拟——大部分标准协议采用此类模拟器。
\item \textbf{回溯模拟器}：可通过重置敌手 $\Adv$ 的内部状态、重新选择随机挑战，强制敌手的行为与模拟器的猜测对齐——主要用于零知识证明类协议的模拟证明。
\end{itemize}

% \section{承诺协议：Pedersen 承诺}
% 承诺协议是密码学的基础原语，对应现实世界的“密封投票”：发送方先将消息加密承诺（不可篡改、不泄露内容），后续公开消息验证承诺的合法性。Pedersen承诺是目前应用最广泛的同态承诺协议。

% \subsection{系统模型}
% \begin{sysmodel}
% Pedersen承诺协议包含两个参与方：
% \begin{itemize}
% \item 承诺方（Sender）：掌握私密消息 $m$，执行承诺、打开流程；
% \item 验证方（Receiver）：获取承诺值，后续验证打开值的合法性。
% \end{itemize}
% 协议分为两个阶段，共3个PPT算法：
% \begin{enumerate}
% \item \textbf{参数生成阶段}：$\Gen(1^\kappa)\to(\mathsf{pp})$：输入安全参数 $\kappa$，输出全局公开参数 $\mathsf{pp}$；
% \item \textbf{承诺阶段}：$\Com_{\mathsf{pp}}(m;r)\to c$：输入消息 $m$、随机盲化因子 $r$，输出承诺值 $c$；
% \item \textbf{打开/验证阶段}：$\Verify_{\mathsf{pp}}(c,m,r)\to\{0,1\}$：输入原承诺值 $c$、消息 $m$、因子 $r$，验证通过输出1，否则输出0。
% \end{enumerate}
% \end{sysmodel}

% \begin{remark}
% Pedersen承诺具备\textbf{同态加法属性}：$\Com(m_1;r_1)\cdot\Com(m_2;r_2)=\Com(m_1+m_2;r_1+r_2)$，该属性在后续零知识证明、数字签名协议中被大量复用。
% \end{remark}

% \subsection{安全模型（基于模拟的形式化定义）}
% 承诺协议必须满足两条核心安全属性，两条属性均采用基于模拟的范式严格定义，分别对抗两类不同的恶意敌手。

% \subsubsection{基于模拟的绑定性（Simulation-Based Binding）}
% 绑定性对抗\textbf{恶意承诺方}：要求承诺方无法将同一个承诺值 $c$，成功打开为两个不同的消息 $m_1\neq m_2$，即承诺值一旦生成，其绑定的消息不可被篡改。

% \begin{secmodel}[绑定性]
% 协议满足基于模拟的绑定性，当且仅当对任意PPT恶意承诺方 $\Adv$，存在PPT模拟器 $\Sim$，使得理想世界中 $\Sim$ 复刻的“篡改攻击视图”，与真实世界中 $\Adv$ 的实际攻击视图计算不可区分。
% 直观逻辑：如果真实世界敌手能篡改承诺绑定的消息，那么模拟器在理想世界也能复刻该篡改行为，但理想世界TTP天然保证绑定性，因此敌手的篡改概率可忽略。
% \end{secmodel}

% \subsubsection{基于模拟的隐藏性（Simulation-Based Hiding）}
% 隐藏性对抗\textbf{恶意验证方}：要求承诺值 $c$ 不泄露任何关于消息 $m$ 的明文信息，恶意验证方无法从承诺值中反推出消息的有效信息。

% \begin{secmodel}[隐藏性]
% 协议满足基于模拟的隐藏性，当且仅当对任意PPT恶意验证方 $\Adv$，存在PPT模拟器 $\Sim$，使得对任意两个等长消息 $m_0,m_1$，真实世界中 $\Adv$ 收到的承诺视图 $\View_\Adv^\Real(m_b)$，与模拟器在理想世界中生成的模拟视图 $\View_\Sim^\Ideal(m_b)$ 计算不可区分（$b\in\{0,1\}$ 为随机挑战比特）。
% \end{secmodel}

% \subsection{方案构造}
% Pedersen承诺的安全性严格归约到\textbf{离散对数（DL）困难假设}，其构造逻辑基于循环群中的指数运算单向性。

% \subsubsection{公共参数生成}
% \begin{construction}[参数生成]
% 算法 $\Gen(1^\kappa)$ 执行流程：
% \begin{enumerate}
% \item 输入安全参数 $\kappa$，生成一个大素数 $q$ 阶的循环群 $\mathbb{G}$；
% \item 随机选取 $\mathbb{G}$ 的两个生成元 $g,h\in\mathbb{G}$，且满足 $h=g^\alpha$，其中 $\alpha\gets\mathbb{Z}_q^*$ 是保密的离散对数对；
% \item 输出公开参数 $\mathsf{pp}=(\mathbb{G},q,g,h)$，保密删除 $\alpha$。
% \end{enumerate}
% \end{construction}

% \subsubsection{承诺与验证算法}
% \begin{construction}[承诺/验证算法]
% \begin{itemize}
% \item 承诺算法 $\Com(m;r)$：
%   输入消息 $m\in\mathbb{Z}_q$、随机盲化因子 $r\gets\mathbb{Z}_q^*$，计算承诺值：
%   \[
%   c = g^m \cdot h^r \in\mathbb{G}
%   \]
% \item 验证算法 $\Verify(c,m,r)$：
%   重新计算 $c'=g^m\cdot h^r$，若 $c'=c$ 输出1，否则输出0。
% \end{itemize}
% \end{construction}

% \subsubsection{ Concrete数值样例}
% 为了便于手动验证，这里给出一个安全参数极小的实际运行样例：
% \begin{example}
% 取安全参数 $\kappa=4$，生成素数阶循环群 $\mathbb{G}=\mathbb{Z}_{23}^*$（模23的乘法循环群，阶 $q=11$），生成元 $g=5$；随机选取 $\alpha=3$，计算 $h=g^\alpha\mod23=5^3\mod23=10$；公开参数 $\mathsf{pp}=(23,11,5,10)$。

% 承诺方选择消息 $m=4$，随机因子 $r=7$，计算承诺值：
% \[
% c = g^m\cdot h^r \mod23 = 5^4 \cdot 10^7 \mod23 = 4 \cdot 12 \mod23 = 2
% \]

% 打开阶段，承诺方公开 $m=4,r=7$，验证方重新计算：
% \[
% c' =5^4\cdot10^7\mod23=4\cdot12\mod23=2=c
% \]
% 验证通过。
% \end{example}

% \subsection{完整安全证明}
% Pedersen承诺的安全性归约到离散对数困难假设：若存在敌手破坏协议安全，则可构造算法破解离散对数问题。

% \subsubsection{绑定性证明}
% \begin{fullproof}[绑定性]
% 采用反证法证明：假设存在PPT恶意承诺方 $\Adv$，以不可忽略概率 $\varepsilon(\kappa)$ 破坏绑定性，即 $\Adv$ 能输出两组合法消息 $(m,r)\neq(m',r')$，满足 $g^m\cdot h^r = g^{m'}\cdot h^{r'}$。

% 基于该敌手，可构造PPT算法 $\mathcal{B}$ 破解离散对数问题：
% \begin{enumerate}
% \item 挑战者给出离散对数挑战实例：给定 $g,h\in\mathbb{G}$，求 $\alpha=\log_g h$；
% \item 算法 $\mathcal{B}$ 将公开参数 $\mathsf{pp}=(\mathbb{G},q,g,h)$ 发送给敌手 $\Adv$；
% \item $\Adv$ 输出两个冲突的合法打开值 $(m,r),(m',r')$，满足 $g^m\cdot h^r = g^{m'}\cdot h^{r'}$；
% \item 整理等式：$g^{m-m'} = h^{r'-r}$，两边取以 $g$ 为底的对数得：
%   \[
%   m - m' = \alpha\cdot(r'-r) \mod q
%   \]
% \item 算法 $\mathcal{B}$ 直接计算出离散对数结果：
%   \[
%   \alpha = \frac{m - m'}{r' - r} \mod q
%   \]
% \end{enumerate}

% 若 $\Adv$ 的攻击概率 $\varepsilon(\kappa)$ 不可忽略，则算法 $\mathcal{B}$ 破解离散对数的概率同样不可忽略，这与离散对数困难假设矛盾。因此，不存在这样的敌手 $\Adv$，Pedersen承诺满足基于模拟的绑定性。
% \end{fullproof}

% \subsubsection{隐藏性证明}
% \begin{fullproof}[隐藏性]
% 构造模拟器 $\Sim$ 证明视图不可区分：
% \begin{enumerate}
% \item 模拟器 $\Sim$ 接收恶意验证方 $\Adv$ 发送的挑战消息 $m_0,m_1$；
% \item $\Sim$ 随机选择一个挑战比特 $b\gets\{0,1\}$，再随机选取一个盲化因子 $r'\gets\mathbb{Z}_q^*$，计算模拟承诺值：
%   \[
%   c^* = g^{m_b}\cdot h^{r'}
%   \]
% \item $\Sim$ 将 $c^*$ 发送给敌手 $\Adv$，后续输出 $\Adv$ 的所有交互信息作为模拟视图。
% \end{enumerate}

% 分析两个视图的分布差异：
% - 在真实世界中，盲化因子 $r$ 均匀随机分布在 $\mathbb{Z}_q^*$ 上，因此真实承诺值 $c=g^{m_b}\cdot h^r$ 在群 $\mathbb{G}$ 上均匀随机分布；
% - 在理想世界中，模拟器选取的 $r'$ 同样均匀随机分布在 $\mathbb{Z}_q^*$ 上，因此模拟承诺值 $c^*=g^{m_b}\cdot h^{r'}$ 同样在群 $\mathbb{G}$ 上均匀随机分布。

% 真实视图与模拟视图的分布完全重合，因此对任意PPT区分器，区分优势严格为0，自然满足计算不可区分。Pedersen承诺满足基于模拟的隐藏性。
% \end{fullproof}

\section{承诺协议}
\subsection {引言}
承诺协议是一套涉及两个密码学参与方（发送方 S 和接收方 R）的两阶段交互密码协议，其核心设计意图可以通过一个通俗的物理场景来直观理解：想象发送方将一个私密信息写入一张纸条，将纸条放入一个上好了锁的保密盒子中，再将这个盒子交付给接收方保管。这一动作对应协议的第一阶段 ——\textbf {承诺阶段}：发送方对信息完成 “承诺”，接收方拿到盒子，但无法打开它，因此无法获知被承诺的信息内容。在后续的某个预定时间点，协议将进入第二阶段 ——\textbf {揭示阶段}：发送方将打开盒子的钥匙交给接收方，接收方使用钥匙打开盒子、取出纸条，便能验证纸条上的信息确实是发送方最初承诺的内容。
从这个直观场景中，我们可以提炼出承诺协议必须满足的两条核心安全属性，这也是本讲义后续将形式化证明的核心目标：
\begin {itemize}
\item \textbf {隐藏性（Hiding）：} 在承诺阶段完成后，接收方在没有获取额外信息的前提下，无法获知任何关于发送方所承诺消息的内容。
\item \textbf {绑定性（Binding）：} 在承诺阶段完成后，发送方无法通过作弊手段，将自己最初的承诺 “篡改” 为另一个不同的消息；换言之，发送方无法让接收方在揭示阶段接受两个完全不同的有效值，视为对同一个承诺的合法揭示。
\end {itemize}
需要特别指出的是，在密码学的形式化安全框架中，隐藏性和绑定性这两种安全属性，各自对应着三个严格程度依次递增的安全级别：\textbf {完美安全}、\textbf {统计安全} 与 \textbf {计算安全}。这三个级别的安全依赖、敌手能力限制以及应用场景，将在本讲义的后续章节中给出严格、明确的形式化区分。
在实际密码协议设计中，我们往往需要根据业务场景的安全需求，匹配不同安全级别的属性组合。然而，密码学中存在一个基础性的 “不可能结果”：\textit {没有任何一个承诺协议，能够同时满足完美隐藏性和完美绑定性}。这一结论直接决定了，在实际应用中，我们必须根据场景的安全侧重点，构造并使用具有互补安全属性组合的承诺协议。
基于这一逻辑，本讲义将重点深入分析两套应用最广泛、安全属性恰好互补的经典承诺协议方案：
\begin {enumerate}
\item \textbf {Pedersen 承诺协议}：该方案基于离散对数问题这一经典密码学困难假设，是密码学中效率最高、应用最广泛的承诺协议之一。其安全属性组合为 \textit {计算绑定性 + 统计隐藏性}，这意味着它的隐藏性可以抵抗具有无限计算能力的敌手，而绑定性则可以抵抗具有多项式时间计算能力的有效敌手。该方案由 Pedersen 在 1991 年的密码学顶会 CRYPTO 上发表，其最初的设计用途是作为可验证秘密分享协议的基础构件\cite{Pedersen1991}。
\item \textbf {Naor 承诺协议}：该方案基于伪随机生成器这一基础性密码学构件，其安全属性组合为 \textit {统计绑定性 + 计算隐藏性}，与 Pedersen 承诺形成了完美的互补。这意味着它的绑定性可以抵抗具有无限计算能力的敌手，而隐藏性可以抵抗多项式时间计算能力的有效敌手。该方案由 Naor 在 1991 年的密码学顶级期刊《Journal of Cryptology》上发表，其核心设计目标是基于任何单向函数构造出安全的承诺协议\cite{Naor1991}。
\end {enumerate}
这两套方案在安全效率、适用场景、安全假设的苛刻程度上形成了完美的互补覆盖，在不同的应用场景中发挥着不可替代的作用。例如，Pedersen 承诺的统计隐藏属性，使其特别适用于需要长期保证消息机密性的场景；而 Naor 承诺的统计绑定属性，则在需要长期防止承诺方抵赖的场景中具有优势。
本讲义将按照 “模型定义 - 方案构造 - 安全性证明” 的标准密码学分析框架，对这两套方案进行全面、细致的形式化阐述，旨在让读者不仅可以熟练掌握这两套经典方案的技术细节，更能深入理解承诺协议的安全设计逻辑与标准的安全证明方法。
\subsection {系统模型}
承诺协议是一个典型的两方密码交互协议，其系统模型仅包含两个参与方：
\begin {itemize}
\item \textbf {发送方（记为 S）：} 拥有待承诺的私密消息，在协议中执行承诺动作，并在协议的揭示阶段打开承诺，向接收方证明其最初承诺的消息值。
\item \textbf {接收方（记为 R）：} 接收发送方发来的承诺数据，在协议的揭示阶段，使用预设的验证逻辑，对发送方揭示的承诺进行合法性校验。
\end {itemize}
该协议在执行逻辑上明确分为两个顺序执行的核心阶段 ——\textit {承诺阶段} 和 \textit {揭示阶段}。为了保证协议的安全性，这两个阶段必须在逻辑上严格区分，先完整执行承诺阶段，再执行揭示阶段，不允许出现阶段交织或颠倒的情况。
下面我们给出这两个阶段的形式化抽象描述，这是后续定义安全模型和构造具体协议的基础。
\begin {definition}[承诺协议]
一个承诺协议 Com = (Commit, Reveal) 由以下两个标准的概率多项式时间（PPT，即敌手的有效计算能力）算法构成：
\begin {enumerate}
\item \textbf {承诺阶段 Commit：} 这是一个两方交互算法，其完整的执行逻辑为：发送方 S 将待承诺的消息\(v \in \mathcal{V}\)作为输入，其中\(\mathcal{V}\)是协议约定的消息空间；接收方 R 可以根据协议要求，输入其本地的随机辅助信息。在双方完成规定的交互步骤后，S 输出最终的承诺值\(c \in \mathcal{C}\)，以及自己留存的一组局部辅助信息\(d_S\)；R 则输出一组用于后续验证承诺的局部辅助信息\(d_R\)。其中\(\mathcal{C}\)是协议规定的承诺值空间。
从形式化角度讲，承诺阶段的交互过程可以表示为：
\((c, d_S, d_R) \leftarrow \text{Commit}(S(v), R())\)
这一过程中，发送方和接收方之间可能会进行多轮的消息交互，具体的交互流程由协议的具体方案构造决定。
\item \textbf {揭示阶段 Reveal：} 这是一个由接收方执行的确定性验证算法，其输入为接收方在承诺阶段获取的承诺值\(c\)、发送方在揭示阶段公开的原消息\(v\)以及发送方留存的局部辅助信息\(d_S\)，还有接收方自己在承诺阶段生成的辅助验证信息\(d_R\)。该算法的输出是一个二进制的验证判定结果\(b \in \{0, 1\}\)，其中\(b=1\)表示接收方接受这个揭示为合法有效的承诺打开，\(b=0\)表示接收方拒绝这个打开承诺。
形式化上，这一验证过程可以表示为：
\(b \leftarrow \text{Reveal}(c, v, d_S, d_R)\)
在非交互式的承诺协议中，揭示阶段的所有信息都由发送方一次性发送给接收方，接收方本地执行验证算法，不需要再和发送方进行额外的交互。
\end {enumerate}
\end {definition}
关于这个系统模型，有几个极其重要的技术细节需要特别强调，理解这些细节是后续理解协议安全性的前提：
\begin {itemize}
\item \textbf {公开参数：} 对于一个具体的承诺协议方案，在协议正式执行之前，通常需要由可信第三方生成并向所有参与方公布一组公开的协议参数。这类公开参数不会影响协议的安全性，也不需要由任何参与方保密，所有参与方都可以获取、校验并使用这些参数。在本讲义后续介绍的 Pedersen 承诺和 Naor 承诺方案中，将分别详细说明其公开参数的生成逻辑和校验方法。
\item \textbf {非交互性与交互性：} 承诺协议可以根据承诺阶段的交互轮次分为非交互承诺和交互承诺两类。非交互承诺指的是在承诺阶段，发送方仅需向接收方发送一条消息，交互就可以完成；而交互承诺则需要发送方和接收方之间进行多轮的消息交互和参数校验。本讲义后续介绍的 Pedersen 承诺属于非交互承诺，其交互效率更高，更适合在网络延迟较高的场景中使用；而 Naor 承诺则属于交互承诺，需要接收方在承诺阶段先向发送方发送一条辅助消息，再完成承诺流程。
\item \textbf {算法的计算能力限制：} 构成承诺协议的 Commit 和 Reveal 两个算法，都必须是概率多项式时间算法。这一要求的实际含义是：这两个算法的执行时间，都不会超过输入消息长度的某个多项式倍数；换言之，算法的执行效率必须是 “高效可行” 的，不能存在指数级别的计算复杂度，保证协议可以在实际的算力资源下正常运行。
\end {itemize}
\subsection {安全模型与安全性定义}
在密码学的形式化安全框架中，承诺协议的安全属性根据敌手能力的不同，严格分为三个安全级别，这三个级别的安全强度依次递增，分别对应着不同的抗敌手能力：
\begin {enumerate}
\item \textbf {计算安全（Computational Security）：} 该级别的安全保护仅能抵抗具有 \textit {多项式时间计算能力} 的有效敌手。也就是说，任何多项式时间运行的敌手，都只能以一个极其微小的、可以忽略的概率成功攻破协议的安全保护。这是在计算困难性假设（比如离散对数假设、因数分解假设）下，密码学方案能实现的最主流的安全级别。
\item \textbf {统计安全（Statistical Security）：} 该级别的安全保护可以抵抗具有 \textit {无限计算能力} 的敌手，不论敌手拥有多强的算力资源，都无法攻破协议的安全保护。但这一安全级别有一个前提限制：敌手只能拥有协议执行过程中公开的、多项式规模的交互数据，不能获取参与方的局部私密信息。
\item \textbf {完美安全（Perfect Security）：} 这是强度最高的安全级别，它和统计安全的抗敌手能力完全一致，但额外要求，协议的安全性分析不依赖于任何统计上的 “可忽略概率” 假设，即不存在任何微小的安全漏洞概率，是完全、绝对的安全。
\end {enumerate}
基于这三个安全级别，我们可以将承诺协议的两个核心安全属性（隐藏性和绑定性），分别形式化为对应的安全等级定义。在实际应用中，最常用的安全属性组合是 \textit {计算绑定 + 统计隐藏}，以及 \textit {统计绑定 + 计算隐藏}，这也正是本讲义后续将详细介绍的 Pedersen 方案和 Naor 方案分别实现的目标。
需要特别强调的是，在密码学的安全证明逻辑中，这两个安全属性是通过两个完全独立的安全实验来定义的，即我们分别针对 “恶意的接收方” 和 “恶意的发送方” 两类不同的攻击场景，分别设计实验，验证协议是否满足对应的安全属性，这两类敌手的攻击目标完全不同。
\subsection {隐藏性（Hiding）}
隐藏性是针对 \textit {恶意接收方} 的安全属性，其核心目标是保证接收方无法在承诺阶段完成后，未打开承诺之前，获取到被承诺消息的任何相关信息。更严格地说，隐藏性要求：即使接收方不遵守协议的规则流程，试图通过自己接收到的承诺值来反向推断发送方承诺的消息内容，也无法有效获取任何有价值的信息。
在密码学的形式化证明框架中，隐藏性是通过一个名为\(\text{Exp}_{\text{Com}}^{\text{hide}}\)的安全实验来形式化定义的，这个实验的执行逻辑如下：
\begin {definition}[隐藏性安全实验]
对于给定的承诺协议\(\text{Com} = (\text{Commit}, \text{Reveal})\)、恶意接收方敌手\(\mathcal{A}\)，以及安全参数\(\kappa\)（即决定协议安全强度的核心参数，一般取 128、256 等二进制长度值），隐藏性安全实验\(\text{Exp}_{\text{Com}}^{\text{hide}}(\mathcal{A}, \kappa)\)的执行流程为：
\begin {enumerate}
\item \textbf {挑战消息生成：} 实验环境首先随机选择两个完全相同长度的消息\(v_0, v_1 \in \mathcal{V}\)，这两个消息将作为发送方的候选承诺消息。
\item \textbf {承诺阶段执行：} 实验环境模拟诚实的发送方\(\text{Commit}\)算法，根据敌手\(\mathcal{A}\)的交互响应，生成对应消息\(v_b\)的承诺值\(c\)，其中\(b \in \{0,1\}\)是实验环境随机选择的一个挑战比特值。
\item \textbf {敌手猜测输出：} 恶意接收方敌手\(\mathcal{A}\)在获取到承诺值\(c\)后，在其执行时间上限内，输出一个对挑战比特\(b\)的猜测值\(b' \in \{0,1\}\)。
\item \textbf {实验结果判定：} 如果敌手的猜测值\(b'\)和实验环境选择的真实比特值\(b\)相等，即\(b'=b\)，则实验输出为 1，代表敌手攻击成功；反之，若\(b' \neq b\)，则实验输出为 0，代表敌手攻击失败。
\end {enumerate}
\end {definition}
基于这个实验，我们可以进一步量化定义敌手的攻击优势，进而给出不同级别的隐藏性安全属性的形式化定义：
\begin {definition}[隐藏性]
给定承诺协议\(\text{Com}\)，安全参数\(\kappa\)，以及恶意接收方敌手\(\mathcal{A}\)，我们定义\(\mathcal{A}\)在攻击该协议的隐藏性时的优势为：
\(\text{Adv}_{\text{Com}}^{\text{hide}}(\mathcal{A}, \kappa) = \left| \Pr\left[\text{Exp}_{\text{Com}}^{\text{hide}}(\mathcal{A}, \kappa) = 1\right] - \frac{1}{2} \right|\)
基于该优势，我们可以定义三个级别的隐藏性安全属性：
\begin {enumerate}
\item \textbf {统计隐藏性：} 对于任意一个可以在多项式时间内运行的恶意接收方敌手\(\mathcal{A}\)，其攻击该协议的隐藏性优势\(\text{Adv}_{\text{Com}}^{\text{hide}}(\mathcal{A}, \kappa)\)，当安全参数\(\kappa\)足够大时，是一个可以忽略的函数。
\item \textbf {计算隐藏性：} 对于任意一个具有无限计算能力的恶意接收方敌手\(\mathcal{A}\)，其攻击该协议的隐藏性优势\(\text{Adv}_{\text{Com}}^{\text{hide}}(\mathcal{A}, \kappa)\)，当安全参数\(\kappa\)足够大时，是一个可以忽略的函数。
\item \textbf {完美隐藏性：} 对于任意的敌手\(\mathcal{A}\)，其攻击该协议的隐藏性优势\(\text{Adv}_{\text{Com}}^{\text{hide}}(\mathcal{A}, \kappa)\)，都精确等于 0，即敌手完全没有任何优势，和随机猜测的结果没有任何区别。
\end {enumerate}
\end {definition}
这里需要对 “可忽略函数” 这一密码学基础概念做一个补充说明：如果一个函数\(\epsilon(\kappa)\)，对于任意一个正多项式\(p(\cdot)\)，都存在一个对应的安全参数阈值\(\kappa_0\)，使得当安全参数\(\kappa > \kappa_0\)时，始终有\(\epsilon(\kappa) < \frac{1}{p(\kappa)}\)成立，那么我们就称这个函数\(\epsilon(\kappa)\)是可忽略的。通俗来讲，可忽略函数的衰减速度，比任何多项式的增长速度都要快，当安全参数达到一定规模后，这个函数的值会变得极其微小，以至于在实际应用中完全可以忽略。
在这个实验框架下，隐藏性的本质可以被理解为：\textit {承诺值\(c\)的概率分布，与被承诺的消息\(v\)的内容是统计独立的}。换句话说，接收方拿到承诺值\(c\)后，无法根据\(c\)的任何特征，区分出\(c\)是对应消息\(v_0\)的承诺值，还是对应消息\(v_1\)的承诺值 —— 这正是 “接收方无法获取承诺消息的任何信息” 这一安全目标的形式化表达。
\subsection {绑定性（Binding）}
绑定性是针对 \textit {恶意发送方} 的安全属性，其核心目标是保证发送方无法 “作弊”—— 即无法在承诺阶段完成后，通过篡改自己的局部辅助信息，将同一个承诺值\(c\)，“打开” 为两个不同的有效值\(v\)和\(v'\)。更严格地说，绑定性要求：即使发送方不遵守协议的规则流程，在揭示阶段伪造验证信息，也无法让接收方接受两个不同的消息为同一个承诺值的合法揭示结果。
和隐藏性类似，绑定性的形式化定义，也是通过一个对应的安全实验\(\text{Exp}_{\text{Com}}^{\text{bind}}\)来给出的：
\begin {definition}[绑定性安全实验]
对于给定的承诺协议\(\text{Com} = (\text{Commit}, \text{Reveal})\)、恶意发送方敌手\(\mathcal{A}\)，以及安全参数\(\kappa\)，绑定性安全实验\(\text{Exp}_{\text{Com}}^{\text{bind}}(\mathcal{A}, \kappa)\)的执行流程为：
\begin {enumerate}
\item \textbf {恶意承诺执行：} 恶意发送方敌手\(\mathcal{A}\)与诚实的接收方进行交互，执行承诺阶段的协议流程。在交互过程中，敌手可以根据自己的攻击策略，随意构造发送给接收方的消息；而诚实的接收方则严格按照协议规则生成自己的局部辅助信息\(d_R\)。
\item \textbf {承诺伪造结果输出：} 交互结束后，敌手\(\mathcal{A}\)输出一个三元组\((c, v, v', d_S, d'_S)\)，其中\(c\)是敌手在承诺阶段发送给接收方的承诺值，\(v\)和\(v'\)是两个不同的消息（即\(v \neq v'\)），\(d_S\)和\(d'_S\)是敌手伪造的、用于打开承诺值\(c\)的两组局部辅助验证信息。
\item \textbf {验证结果判定：} 实验环境调用诚实的接收方所使用的\(\text{Reveal}\)验证算法，分别对这两组揭示信息进行校验，得到两个验证结果\(b = \text{Reveal}(c, v, d_S, d_R)\)和\(b' = \text{Reveal}(c, v', d'_S, d_R)\)。如果这两个验证结果都为 1，即\(b=b'=1\)，则实验输出为 1，代表敌手攻击成功；反之，实验输出为 0，代表敌手攻击失败。
\end {enumerate}
\end {definition}
基于这个实验，我们可以同样量化定义敌手的攻击优势，进而给出不同级别的绑定性安全属性的形式化定义：
\begin {definition}[绑定性]
给定承诺协议\(\text{Com}\)，安全参数\(\kappa\)，以及恶意发送方敌手\(\mathcal{A}\)，我们定义\(\mathcal{A}\)在攻击该协议的绑定性时的优势为：
\(\text{Adv}_{\text{Com}}^{\text{bind}}(\mathcal{A}, \kappa) = \Pr\left[\text{Exp}_{\text{Com}}^{\text{bind}}(\mathcal{A}, \kappa) = 1\right]\)
基于该优势，我们可以定义三个级别的绑定性安全属性：
\begin {enumerate}
\item \textbf {统计绑定性：} 对于任意一个可以在多项式时间内运行的恶意发送方敌手\(\mathcal{A}\)，其攻击该协议的绑定性优势\(\text{Adv}_{\text{Com}}^{\text{bind}}(\mathcal{A}, \kappa)\)，当安全参数\(\kappa\)足够大时，是一个可以忽略的函数。
\item \textbf {计算绑定性：} 对于任意一个具有无限计算能力的恶意发送方敌手\(\mathcal{A}\)，其攻击该协议的绑定性优势\(\text{Adv}_{\text{Com}}^{\text{bind}}(\mathcal{A}, \kappa)\)，当安全参数\(\kappa\)足够大时，是一个可以忽略的函数。
\item \textbf {完美绑定性：} 对于任意的敌手\(\mathcal{A}\)，其攻击该协议的绑定性优势\(\text{Adv}_{\text{Com}}^{\text{bind}}(\mathcal{A}, \kappa)\)，都精确等于 0，即敌手完全不可能构造出这样的 “碰撞” 揭示结果。
\end {enumerate}
\end {definition}
从这一形式化定义可以看出，绑定性的本质，实际上是要求承诺算法\(\text{Commit}\)具有某种 “抗碰撞性”—— 也就是说，找到两个不同的消息\(v\)和\(v'\)，以及对应的随机辅助信息\(d_S\)和\(d'_S\)，使得这两组输入能生成完全相同的承诺值\(c\)，这件事在计算上是不可行的，或是在统计上几乎不可能的。
\subsection {安全属性的不可能性结果}
在介绍具体的协议方案之前，我们必须先介绍密码学中关于承诺协议的一个基础性的不可能性结果，这是理解后续方案安全属性设计逻辑的关键前提。
\begin {theorem}[不可能性结果]
不存在同时满足完美隐藏性和完美绑定性的承诺协议。
\end {theorem}
这一结论的逻辑证明思路并不复杂：假设存在一个同时满足完美隐藏性和完美绑定性的承诺协议，那么根据完美隐藏性的定义，无论承诺值\(c\)是什么，它都和任意一个被承诺的消息\(v\)的内容统计独立；换句话说，对于任意的承诺值\(c\)，以及任意的消息\(v \in \mathcal{V}\)，都必然存在一组对应的局部辅助信息\(d_S\)，使得\(\text{Reveal}(c, v, d_S, d_R)=1\)成立。但这一结论，直接和完美绑定性的定义相矛盾 —— 完美绑定性要求，对于同一个承诺值\(c\)，不可能存在两个不同的消息\(v\)和\(v'\)，使得它们的验证结果同时为 1。
这一不可能性结果，直接决定了在实际的密码协议设计场景中，我们无法构造出一个同时具备最强级别隐藏性和最强级别绑定性的承诺协议。因此，在实际应用中，我们必须根据业务场景的安全需求优先级，选择合适的安全属性组合。而这也正是本讲义后续将介绍 Pedersen 方案和 Naor 方案的核心原因：这两套方案分别实现了两种最常用、互补的安全属性组合，足以覆盖绝大多数实际应用场景的安全需求。
\subsection {方案构造 I：Pedersen 承诺协议（计算绑定 + 统计隐藏）}
Pedersen 承诺协议是 1991 年由密码学家 Torben P. Pedersen 在密码学顶级会议 CRYPTO 上发表的经典方案，是目前已知的效率最高、应用最广泛的承诺协议～\cite {Pedersen1991}。该方案构造在循环群的密码学困难假设之上，其安全属性组合为 \textit {统计隐藏性 + 计算绑定性}，是一种典型的 “非交互式” 承诺协议 —— 在承诺阶段，发送方仅需给接收方发送一条消息，交互就可以完成。这一特性使得该协议在网络延迟较高或通信轮次受限的场景中，具有天然的应用优势。
\subsubsection {前提假设与公开参数生成}
Pedersen 承诺协议的安全性依赖于经典的 \textit {离散对数困难性假设}，这是数论中一个被广泛研究和认可的密码学困难假设。在构造协议之前，我们需要先定义一个满足该假设的循环群，以及相关的公开全局参数。
\begin {definition}[循环群与生成元]
设\(\mathbb{G}\)是一个阶为素数\(q\)的有限循环群，其中\(q\)的二进制长度就是该协议的安全参数\(\kappa\)。这意味着，在实际应用中，\(q\)一般是一个长度为 2048 位或 4096 位的大素数，以保证离散对数问题的求解难度足够高。设\(g\)是循环群\(\mathbb{G}\)的一个生成元，这意味着\(g\)可以通过幂次运算生成群\(\mathbb{G}\)中的所有元素。
\end {definition}
基于这个循环群，我们可以定义 Pedersen 承诺协议的公开参数生成逻辑。需要特别强调的是，这些公开参数的生成过程必须由可信第三方来执行，或者由协议的所有参与方协同执行一个 “参数生成协议” 来生成，以确保参数不会被恶意第三方篡改。
\begin {construction}[Pedersen 承诺公开参数]
Pedersen 承诺协议的公开参数由可信第三方按照以下逻辑生成：
\begin {enumerate}
\item 选择一个安全的循环群\(\mathbb{G}\)，其阶为大素数\(q\)，生成元为\(g\)；
\item 从群\(\mathbb{G}\)中随机选择一个元素\(h\)，且保证\(h \neq g\)。为了保证参数的安全性，我们要求\(h\)是\(g\)的一个随机幂次值，即存在一个随机数\(x \in \mathbb{Z}_q^*\)，使得\(h = g^x\)，但\(x\)这个值必须由可信第三方在生成完成后立即销毁，不能向任何参与方公开，也不能由任何参与方推算出来。
\end {enumerate}
最终，协议的公开参数集合为\(\text{params} = (\mathbb{G}, q, g, h)\)，所有参与方都可以获取这份公开参数集合。
\end {construction}
在实际应用中，这类公开参数往往是由标准组织或行业领军企业提前生成并公开的，比如在区块链应用中，Pedersen 承诺的公开参数一般会作为链的创世参数一部分，被所有节点同步校验。
\subsubsection {协议构造}
Pedersen 承诺协议是一个非交互式协议，其承诺阶段不需要接收方发送任何辅助消息，协议的完整流程由以下两个算法组成。
\begin {construction}[Pedersen 承诺协议]
给定公开参数\(\text{params} = (\mathbb{G}, q, g, h)\)，Pedersen 承诺协议的两个核心算法的执行逻辑为：
\begin {enumerate}
\item \textbf {承诺阶段\(\text{Commit}\)：} 发送方 S 待承诺的消息必须是一个位于有限域\(\mathbb{Z}_q\)中的元素，即\(v \in \mathbb{Z}_q\)。这里的\(\mathbb{Z}_q\)表示的是从 0 到\(q-1\)的所有整数构成的集合，集合中的元素加法、乘法运算结果均需要对素数\(q\)取模。如果实际待承诺的消息是二进制字符串或其他数据格式，发送方需要先将其编码为\(\mathbb{Z}_q\)中的元素格式。在正式生成承诺值之前，发送方需要先从\(\mathbb{Z}_q\)中随机选取一个秘密的随机数\(r\)，这个随机数被称为 “承诺随机数”，将作为发送方的局部辅助信息留存，不会被公开给接收方或其他第三方。随后，发送方使用自己的私密随机数\(r\)和消息\(v\)，按照以下公式计算出承诺值\(c\)：
\(c = g^v \cdot h^r\)
这里的幂次运算和乘法运算，都是在循环群\(\mathbb{G}\)中的运算规则下执行的。计算完成后，发送方将生成的承诺值\(c\)发送给接收方。此时，发送方的局部辅助信息为\(d_S = (v, r)\)，即待承诺的消息和私密随机数；而接收方收到承诺值\(c\)后，将其作为自己的局部辅助信息，即\(d_R = c\)。
\item \textbf {揭示阶段\(\text{Reveal}\)：} 当需要打开承诺时，发送方将自己承诺的消息\(v\)，以及留存的私密随机数\(r\)，一并发送给接收方。接收方在收到这两个值后，首先需要验证\(v\)和\(r\)都是\(\mathbb{Z}_q\)中的合法元素，随后使用公开参数中的\(g\)、\(h\)，以及自己在承诺阶段获取的承诺值\(c\)，按照和承诺阶段完全一致的计算逻辑，重新计算一遍承诺值\(c' = g^v \cdot h^r\)。接下来，接收方将重新计算的承诺值\(c'\)，和自己在承诺阶段收到的原始承诺值\(c\)进行恒等校验：如果\(c' = c\)，则验证算法输出\(b=1\)，代表接收方认可这个揭示结果是合法有效的；反之，如果\(c' \neq c\)，则验证算法输出\(b=0\)，代表接收方拒绝这个揭示结果，认为其不合法。
\end {enumerate}
\end {construction}
需要特别强调的是，在这个协议中，随机数\(r\)的保密性和不可预测性，是整个协议安全性的核心基础。如果发送方在生成不同的承诺值时使用了相同的随机数\(r\)，或者\(r\)被第三方推算出来，那么整个协议的隐藏性都会被直接攻破，第三方可以从承诺值\(c\)中直接获取到被承诺消息\(v\)的内容。
\subsubsection {基于模拟的安全性证明}
我们将采用现代密码学中标准的基于模拟的证明范式，来证明 Pedersen 承诺协议确实满足 \textit {统计隐藏性} 和 \textit {计算绑定性} 的安全属性组合。基于模拟的证明范式的核心逻辑是：对于任何一个真实环境下的恶意敌手\(\mathcal{A}\)，我们都可以构造一个对应的模拟器\(\text{Sim}\)，该模拟器不拥有任何关于被承诺消息的额外信息，仅通过和敌手\(\mathcal{A}\)的交互，就能模拟出真实协议执行时的所有公开消息内容。如果模拟器生成的模拟执行环境，和真实协议的执行环境是不可区分的，那么就能证明协议在安全属性上是安全的。
\subsubsection {统计隐藏性证明}
\begin {theorem}
Pedersen 承诺协议在基于模拟的安全范式下，满足统计隐藏性安全属性。
\end {theorem}
\begin {proof}
我们需要构造一个有效的模拟器\(\text{Sim}\)，使得对于任意一个恶意的接收方敌手\(\mathcal{A}\)，在真实协议执行环境中，敌手\(\mathcal{A}\)与诚实发送方交互时的视图（即敌手在协议执行过程中获取的所有信息的集合），与在模拟器\(\text{Sim}\)构造的模拟环境中，敌手\(\mathcal{A}\)与模拟器交互时的视图，在统计上是不可区分的。
\textbf {模拟器构造逻辑：} 对于任意一个待承诺的消息\(v^* \in \mathbb{Z}_q\)，模拟器\(\text{Sim}\)在不掌握任何关于\(v^*\)的私密信息的前提下，仅通过公开参数，即可按照以下逻辑生成一个和真实承诺值统计上不可区分的模拟承诺值\(c\)：
\begin {enumerate}
\item 从\(\mathbb{Z}_q\)中随机选取一个随机数\(r'\)；
\item 直接计算模拟承诺值\(c' = h^{r'}\)，其中\(h\)是公开参数中的群元素；
\item 将\(c'\)作为模拟的承诺值发送给敌手\(\mathcal{A}\)。
\end{enumerate}
\textbf {不可区分性证明：} 在真实协议执行环境中，当诚实的发送方承诺消息\(v\)时，发送方会使用自己的私密随机数\(r\)计算出承诺值\(c = g^v \cdot h^r\)。由于\(h\)是循环群\(\mathbb{G}\)中的一个元素，根据循环群的生成元性质，必然存在一个指数\(x \in \mathbb{Z}_q^*\)，使得\(h = g^x\)。因此，真实承诺值\(c\)的计算逻辑可以改写为\(c = g^v \cdot (g^x)^r = g^{v + xr}\)。
而在模拟器构造的模拟环境中，模拟器生成的模拟承诺值为\(c' = h^{r'} = g^{x r'}\)。
由于\(r\)和\(r'\)都是从\(\mathbb{Z}_q\)中随机选取的，且循环群\(\mathbb{G}\)的阶\(q\)是一个大素数，因此，\(v + xr\)和\(x r'\)这两个值在\(\mathbb{Z}_q\)中，都是服从均匀分布的随机变量。这意味着，真实承诺值\(c\)和模拟承诺值\(c'\)，在统计上具有完全相同的概率分布。
因此，对于任意的恶意接收方敌手\(\mathcal{A}\)，其在真实协议执行环境中的视图和在模拟环境中的视图是统计不可区分的，二者的统计区分优势完全为 0。根据统计隐藏性的定义，我们可以直接得出结论：Pedersen 承诺协议满足统计隐藏性。
\end {proof}
\subsubsection {计算绑定性证明}
\begin {theorem}
如果在循环群\(\mathbb{G}\)上，离散对数问题是计算困难的，那么 Pedersen 承诺协议在基于模拟的安全范式下，满足计算绑定性安全属性。
\end {theorem}
\begin {proof}
我们采用反证法来证明这一结论。假设 Pedersen 承诺协议不满足计算绑定性，那么这意味着，存在一个概率多项式时间的恶意发送方敌手\(\mathcal{A}\)，可以在绑定性安全实验中，以一个不可忽略的优势\(\epsilon\)，成功输出两个不同的消息\(v\)和\(v'\)，以及对应的两组随机数\(r\)和\(r'\)，使得这两组输入生成的承诺值完全相同，即\(g^v \cdot h^r = g^{v'} \cdot h^{r'}\)。我们可以利用这个能攻破绑定性的敌手\(\mathcal{A}\)，构造出一个能在多项式时间内解决循环群\(\mathbb{G}\)上离散对数问题的算法。
\textbf {归约算法构造逻辑：} 假设我们需要求解循环群\(\mathbb{G}\)上的一个离散对数挑战，即给定\(g\)和\(h\)，找到\(x \in \mathbb{Z}_q^*\)，使得\(h = g^x\)。我们可以按照以下逻辑构造一个离散对数求解算法：
\begin {enumerate}
\item 将公开参数\(\text{params} = (\mathbb{G}, q, g, h)\)作为输入，提供给恶意发送方敌手\(\mathcal{A}\)；
\item 敌手\(\mathcal{A}\)输出一个碰撞结果：两个不同的消息\(v, v' \in \mathbb{Z}_q\)，以及对应的两组随机数\(r, r' \in \mathbb{Z}_q\)，满足\(g^v \cdot h^r = g^{v'} \cdot h^{r'}\)；
\item 由于\(v \neq v'\)，我们可以对上述等式进行变形，最终推导出\(h = g^{\frac{v' - v}{r - r'}}\)。这意味着，我们可以直接计算出\(x = \frac{v' - v}{r - r'}\)，这个值就是\(h\)基于生成元\(g\)的离散对数。
\end {enumerate}
根据我们的假设，敌手\(\mathcal{A}\)可以在多项式时间内，以不可忽略的优势\(\epsilon\)输出这样的一个碰撞结果，那么这意味着，我们构造的这个离散对数求解算法，也可以在多项式时间内，以不可忽略的优势\(\epsilon\)，成功解决循环群\(\mathbb{G}\)上的离散对数问题。但这一结论，和 “循环群\(\mathbb{G}\)上的离散对数问题是计算困难的” 这一前提假设直接矛盾。
因此，我们的初始假设不成立，不存在这样的概率多项式时间敌手\(\mathcal{A}\)可以攻破 Pedersen 承诺协议的绑定性。根据计算绑定性的定义，我们可以得出结论：Pedersen 承诺协议满足计算绑定性。
\end {proof}
\subsection {方案构造 II：Naor 承诺协议（统计绑定 + 计算隐藏）}
Naor 承诺协议是 1991 年由密码学家 Moni Naor 在密码学顶级期刊《Journal of Cryptology》上发表的经典方案～\cite {Naor1991}，其安全属性组合恰好与 Pedersen 承诺协议形成互补 ——\textit {统计绑定性 + 计算隐藏性}。该方案的构造基础是任意一个具有伪随机性的密码学高效伪随机生成器（PRG），这使得该方案可以基于任何单向函数来实例化，在理论密码学中具有非常重要的价值。与 Pedersen 承诺协议不同，Naor 承诺协议是一个交互式承诺协议 —— 在承诺阶段，需要接收方先向发送方发送一条随机的辅助消息，随后发送方再基于这条消息生成承诺值。
\subsubsection {前提假设与公开参数生成}
Naor 承诺协议的安全性依赖于伪随机生成器（PRG）的伪随机性这一基础假设。在构造协议之前，我们需要先明确定义伪随机生成器的技术标准。
\begin {definition}[伪随机生成器]
设\(G: \{0,1\}^{\kappa} \rightarrow \{0,1\}^{2\kappa}\)是一个确定性的密码学算法，其输入是一个长度为\(\kappa\)的二进制随机种子，输出是一个长度为\(2\kappa\)的二进制字符串。如果这个算法\(G\)满足以下两个条件，那么它就是一个安全的伪随机生成器：
\begin {enumerate}
\item \textbf {扩展性：} 输出的二进制字符串长度，严格大于输入的种子长度；
\item \textbf {伪随机性：} 对于任意一个概率多项式时间的算法区分器\(\mathcal{D}\)，其区分 “\(G\)的输出字符串” 和 “真正的随机字符串（长度与\(G\)的输出相同）” 的优势，是一个可忽略的函数。
\end {enumerate}
\end {definition}
基于这个伪随机生成器，我们可以定义 Naor 承诺协议的公开参数。需要特别指出的是，Naor 承诺协议的公开参数非常简单，不需要依赖可信第三方进行复杂的参数生成，仅需要双方认可的伪随机生成器\(G\)和安全参数\(\kappa\)。
\subsubsection {协议构造}
Naor 承诺协议是一个交互式协议，其承诺阶段需要接收方和发送方进行一次消息交互，完整的协议流程由以下两个算法和交互步骤组成。
\begin {construction}[Naor 承诺协议]
给定安全参数\(\kappa\)和伪随机生成器\(G: \{0,1\}^{\kappa} \rightarrow \{0,1\}^{2\kappa}\)，Naor 承诺协议的完整交互流程为：
\begin {enumerate}
\item \textbf {承诺阶段交互步骤 1（接收方→发送方）：} 接收方 R 首先从\(\{0,1\}^{\kappa}\)中随机选取一个长度为\(\kappa\)的二进制字符串\(s\)，作为自己的随机种子。随后，接收方将这个随机种子\(s\)使用伪随机生成器\(G\)进行加密处理，得到一个长度为\(2\kappa\)的加密字符串\(G(s)\)。紧接着，接收方将这个加密后的字符串\(G(s)\)发送给发送方 S，作为承诺阶段的第一条交互消息。此时，接收方需要将自己选择的随机种子\(s\)作为局部私密信息留存，不能发送给发送方或其他第三方。
\item \textbf {承诺阶段交互步骤 2（发送方→接收方）：} 发送方 S 待承诺的消息是一个二进制比特\(v \in \{0,1\}\)。如果实际待承诺的消息是一个长度大于 1 的二进制字符串，发送方可以将消息拆分成多个单独的比特，对每个比特分别执行一次 Naor 承诺协议，最终完成对整个字符串的承诺。在收到接收方发来的加密字符串\(G(s)\)后，发送方从\(\{0,1\}^{\kappa}\)中随机选取一个长度为\(\kappa\)的二进制字符串\(r\)，作为自己的承诺随机数。随后，发送方根据自己待承诺的比特\(v\)，按照以下逻辑计算承诺值\(c\)：
\begin {itemize}
\item 如果待承诺的比特\(v=0\)，那么发送方直接将随机数\(r\)使用伪随机生成器\(G\)进行加密处理，得到的结果\(G(r)\)作为承诺值\(c\)；
\item 如果待承诺的比特\(v=1\)，那么发送方将自己的随机数\(r\)对应的加密字符串\(G(r)\)，与接收方发来的加密字符串\(G(s)\)进行一次逐位异或（XOR）运算，将运算结果\(G(r) \oplus G(s)\)作为承诺值\(c\)。
\end {itemize}
计算完成后，发送方将承诺值\(c\)发送给接收方。此时，发送方的局部辅助信息为\(d_S = (v, r)\)，即待承诺的比特和自己的承诺随机数；而接收方的局部辅助信息为\(d_R = s\)，即自己的随机种子。
\item \textbf {揭示阶段\(\text{Reveal}\)：} 当需要打开承诺时，发送方将自己承诺的消息\(v\)，以及留存的承诺随机数\(r\)，一并发送给接收方。接收方在收到这两个值后，首先会重新计算一遍\(G(r)\)，随后结合自己留存的私密种子\(s\)对应的加密字符串\(G(s)\)，根据\(v\)的值，按照和承诺阶段完全一致的计算逻辑，重新生成一遍承诺值\(c'\)：
\begin {itemize}
\item 如果\(v=0\)，那么接收方计算\(c' = G(r)\)；
\item 如果\(v=1\)，那么接收方计算\(c' = G(r) \oplus G(s)\)。
\end {itemize}
计算完成后，接收方将重新计算的承诺值\(c'\)，和自己在承诺阶段收到的原始承诺值\(c\)进行恒等校验：如果\(c' = c\)，则验证算法输出\(b=1\)，代表接收方认可这个揭示结果是合法有效的；反之，如果\(c' \neq c\)，则验证算法输出\(b=0\)，代表接收方拒绝这个揭示结果，认为其不合法。
\end {enumerate}
\end {construction}
需要特别强调的是，在这个协议中，接收方的随机种子\(s\)的保密性，是整个协议安全性的核心基础。如果发送方在承诺阶段获取到了\(s\)的具体值，那么它就可以轻易地构造出 “碰撞” 承诺值，攻破协议的绑定性；反之，如果接收方在承诺阶段完成后泄露了\(s\)，那么它就可以直接打开发送方的承诺值，获取到被承诺消息的内容，直接攻破协议的隐藏性。
\subsubsection {基于模拟的安全性证明}
我们将继续使用基于模拟的证明范式，来证明 Naor 承诺协议确实满足 \textit {统计绑定性} 和 \textit {计算隐藏性} 的安全属性组合。
\subsubsection {统计绑定性证明}
\begin {theorem}
Naor 承诺协议在基于模拟的安全范式下，满足统计绑定性安全属性。
\end {theorem}
\begin {proof}
根据绑定性的定义，我们需要证明的是，对于任意一个恶意发送方敌手\(\mathcal{A}\)，即使它拥有无限的计算能力，也无法在承诺阶段结束后，找到两个不同的比特值\(v\)和\(v'\)，以及对应的两组随机数\(r\)和\(r'\)，使得这两组输入生成的承诺值完全相同。
我们依然采用反证法来证明这一结论。假设 Naor 承诺协议不满足统计绑定性，那么这意味着，存在一个恶意发送方敌手\(\mathcal{A}\)，可以在承诺阶段结束后，找到两个不同的比特值\(v\)和\(v'\)（即\(v \neq v'\)），以及对应的两组随机数\(r, r' \in \{0,1\}^{\kappa}\)，使得这两组输入生成的承诺值完全相同。
我们分两种情况讨论这一假设下的矛盾关系：
\begin {enumerate}
\item \textbf {情况 1：敌手找到的碰撞满足\(v=0, v'=1\)。} 在这种情况下，根据协议的承诺值计算逻辑，敌手构造的碰撞要求\(G(r) = G(r') \oplus G(s)\)。对该等式两边同时进行逐位异或运算，可以推导出\(G(s) = G(r) \oplus G(r')\)。这意味着，敌手可以在不知道接收方种子\(s\)的情况下，计算出\(G(s)\)的值。但根据伪随机生成器的定义，\(G(s)\)是一个长度为\(2\kappa\)的伪随机字符串，其和一个真正的随机字符串的区分概率是可忽略的。因此，敌手能找到这样的\(r\)和\(r'\)的概率，是完全可以忽略的。
\item \textbf {情况 2：敌手找到的碰撞满足\(v=1, v'=0\)。} 这种情况和情况 1 在逻辑上完全对称，可以推导出完全相同的矛盾关系。
\end {enumerate}
综合以上两种情况，我们可以得出结论：即使恶意发送方敌手\(\mathcal{A}\)拥有无限的计算能力，它能找到这样的碰撞结果的概率，也是一个可忽略的函数。根据统计绑定性的定义，Naor 承诺协议满足统计绑定性。
\end {proof}
\subsubsection {计算隐藏性证明}
\begin {theorem}
如果伪随机生成器\(G\)满足其伪随机性定义要求，那么 Naor 承诺协议在基于模拟的安全范式下，满足计算隐藏性安全属性。
\end {theorem}
\begin {proof}
我们需要构造一个有效的模拟器\(\text{Sim}\)，使得对于任意一个概率多项式时间的恶意接收方敌手\(\mathcal{A}\)，其在真实协议执行环境中的视图，与在模拟器构造的模拟环境中的视图，是计算不可区分的。
\textbf {模拟器构造逻辑：} 模拟器\(\text{Sim}\)按以下逻辑生成模拟的承诺值，与恶意接收方敌手\(\mathcal{A}\)进行交互：
\begin {enumerate}
\item 从\(\{0,1\}^{\kappa}\)中随机选取一个长度为\(\kappa\)的二进制字符串\(r'\)，作为模拟的承诺随机数；
\item 从\(\{0,1\}^{2\kappa}\)中随机选取一个长度为\(2\kappa\)的二进制字符串\(c'\)，作为模拟的承诺值；
\item 将\(c'\)发送给敌手\(\mathcal{A}\)，作为模拟的承诺值。
\end {enumerate}
\textbf {不可区分性证明：} 在真实协议执行环境中，根据待承诺比特\(v\)的不同，承诺值的计算逻辑分为两种：
\begin {itemize}
\item 如果\(v=0\)，则承诺值\(c = G(r)\)，其中\(r\)是发送方的随机数；
\item 如果\(v=1\)，则承诺值\(c = G(r) \oplus G(s)\)，其中\(s\)是接收方的种子。
\end {itemize}
而在模拟环境中，模拟器生成的模拟承诺值\(c'\)是一个完全随机的长度为\(2\kappa\)的二进制字符串。
根据伪随机生成器的伪随机性定义，对于任意一个概率多项式时间的区分器\(\mathcal{A}\)，它区分 “真实的承诺值\(c\)” 和 “模拟的承诺值\(c'\)” 的优势，是一个可忽略的函数。因此，恶意接收方敌手\(\mathcal{A}\)无法在多项式时间内，以一个不可忽略的优势，区分出自己是在和真实的发送方交互，还是在和模拟器交互。
这意味着，敌手\(\mathcal{A}\)在真实协议执行环境中的视图和在模拟环境中的视图，是计算不可区分的。根据计算隐藏性的定义，Naor 承诺协议满足计算隐藏性。
\end {proof}
\subsection {结论}
承诺协议是构造绝大多数高级密码协议的核心基础积木，其安全属性的组合选择，直接决定了上层应用的安全边界。本讲义详细阐述了承诺协议的系统模型、安全模型与形式化的安全属性定义，深入讲解了两套在密码学理论和实际应用中都极为重要的经典方案构造 ——Pedersen 承诺协议和 Naor 承诺协议。
这两套方案的安全属性互补，各自具有明确的适用场景，其核心特征对比如下：
\begin {itemize}
\item \textbf {Pedersen 承诺协议：} 基于经典的离散对数困难性假设，是一种非交互式承诺协议，通信轮次效率高。该方案实现了 \textit {统计隐藏性 + 计算绑定性} 的安全属性组合：统计隐藏性保证了即使敌手拥有无限计算能力，也无法在不打开承诺的情况下，获取承诺消息的任何内容；而计算绑定性则保证了在离散对数假设下，多项式时间算力的敌手无法构造出碰撞承诺值。这一方案特别适用于需要长期保证数据机密性的场景，比如区块链中的隐私交易协议、零知识证明类的隐私计算协议，以及其他需要非交互特性的安全协议场景。
\item \textbf {Naor 承诺协议：} 基于任意一个伪随机生成器这一基础密码学构件，是一种交互式承诺协议，需要通信双方进行一次额外的消息交互。该方案实现了 \textit {统计绑定性 + 计算隐藏性} 的安全属性组合：统计绑定性保证了即使敌手拥有无限计算能力，也无法构造出碰撞承诺值；而计算隐藏性则保证了在伪随机生成器的安全假设下，多项式时间算力的敌手无法在不打开承诺时获取到承诺消息的内容。这一方案的理论意义远大于其实际应用价值，它证明了 “基于任何单向函数类的密码学原语，都可以构造出一个安全的承诺协议” 这一重要结论，是很多理论密码学安全性归约证明的基础构件。
\end {itemize}
掌握这两套方案的技术细节、安全属性差异与适用场景边界，是理解、设计和分析更复杂的密码协议的关键基础。在实际应用中，选择哪套方案，需要根据具体业务场景的安全需求优先级、网络通信条件以及底层安全假设的可实现性来综合判断。

\section{零知识证明：Goldreich-Micali-Rackoff（GMR）图同构协议}
零知识证明（ZKP）是密码学核心原语：证明方能在不泄露任何私密证据的前提下，向验证方证明自己掌握某个私密证据，且证明过程无法被伪造。GMR协议是首个严格满足零知识属性的交互证明协议。

\subsection{系统模型}
\begin{sysmodel}
GMR协议包含两个参与方：
\begin{itemize}
\item 证明方（Prover）：掌握私密证据——两个简单图 $G_0=(V,E_0),G_1=(V,E_1)$ 之间的同构置换 $\pi$，即 $\pi(G_0)=G_1$；
\item 验证方（Verifier）：仅公开输入图 $G_0,G_1$，不掌握置换 $\pi$，与证明方交互验证同构性。
\end{itemize}
协议为三轮公开硬币交互协议（3-move public coin），包含三个核心交互步骤：
\begin{enumerate}
\item 证明方发送第一轮承诺消息（Commitment）；
\item 验证方发送随机挑战比特（Challenge）；
\item 证明方返回挑战响应（Response），验证方执行最终判定。
\end{enumerate}
协议目标：在不泄露 $\pi$ 的前提下，向验证方证明“$G_0$ 与 $G_1$ 同构”的命题为真。
\end{sysmodel}

\subsection{安全模型（基于模拟的零知识定义）}
零知识证明需要满足三条核心安全属性，且全部通过模拟范式形式化定义：
\begin{secmodel}[零知识属性]
协议满足\textbf{零知识属性}，当且仅当对任意PPT恶意验证方 $\Adv$，存在PPT\textbf{回溯模拟器} $\Sim$，使得真实世界中 $\Adv$ 与证明方交互获得的真实视图 $\View_\Adv^\Real$，与理想世界中 $\Sim$ 生成的模拟视图 $\View_\Sim^\Ideal$ 计算不可区分。
模拟器 $\Sim$ 无需掌握私密证据 $\pi$，仅依靠公开的图 $G_0,G_1$ 复刻完整交互视图——这保证了验证方在交互过程中，无法获取任何关于私密置换 $\pi$ 的额外信息。
\end{secmodel}

\begin{secmodel}[完备性]
若证明方确实掌握正确的同构置换 $\pi$，则验证方必然以概率1接受证明——真实执行视图与理想功能视图完全匹配。
\end{secmodel}

\begin{secmodel}[可靠性]
对任意PPT恶意证明方 $\Adv$，若 $G_0$ 与 $G_1$ 实际不同构，则 $\Adv$ 能欺骗验证方接受错误命题的概率不超过 $1/2$，可通过多轮迭代将攻击概率压缩至可忽略级别。
\end{secmodel}

\subsection{方案构造}
GMR协议的构造逻辑基于图置换的承诺与挑战响应机制，完整交互流程如下：

\begin{construction}[GMR协议完整交互]
公共输入：两个 $n$ 节点简单图 $G_0=(V,E_0),G_1=(V,E_1)$；\\
证明方私密输入：同构置换 $\pi:V\to V$，满足 $\pi(G_0)=G_1$；\\
协议单轮执行流程：
\begin{enumerate}
\item \textbf{证明方承诺阶段}：
  \begin{enumerate}
  \item 证明方随机选取一个置换 $\sigma\gets\mathsf{Perm}(V)$（$n$ 阶对称群中的随机置换）；
  \item 计算置换后的同构图 $H=\sigma(G_0)$；
  \item 将图 $H$ 作为承诺消息发送给验证方。
  \end{enumerate}
\item \textbf{验证方挑战阶段}：
  验证方随机生成一个挑战比特 $b\gets\{0,1\}$，发送给证明方。
\item \textbf{证明方响应阶段}：
  证明方根据挑战比特 $b$，计算响应置换 $\rho$：
  \begin{itemize}
  \item 若 $b=0$，证明方直接回复 $\rho=\sigma$；
  \item 若 $b=1$，证明方回复 $\rho=\sigma\circ\pi^{-1}$（复合置换，先执行 $\pi^{-1}$ 再执行 $\sigma$）；
  \end{itemize}
  将响应 $\rho$ 发送给验证方。
\item \textbf{验证方判定阶段}：
  验证方检查置换 $\rho$ 是否满足同构条件：
  \begin{itemize}
  \item 若 $b=0$，验证 $\rho(G_0)\stackrel{?}{=}H$；
  \item 若 $b=1$，验证 $\rho(G_1)\stackrel{?}{=}H$；
  \end{itemize}
  若等式成立则接受证明，否则拒绝。
\end{enumerate}
\end{construction}

\subsubsection{Concrete数值样例}
用一个极小的4节点无向图作为例子，手动演示协议执行逻辑：
\begin{example}
设两个公开输入图 $G_0,G_1$，顶点集 $V=\{1,2,3,4\}$，边集分别为：
- $G_0$ 的边集 $E_0=\{(1,2),(2,3),(3,4),(4,1)\}$（4节点环）；
- $G_1$ 的边集 $E_1=\{(1,3),(3,2),(2,4),(4,1)\}$（另一种形式的4节点环）；

证明方掌握私密同构置换 $\pi=(1\ 2\ 3\ 4)$（轮换置换），满足 $\pi(G_0)=G_1$。

协议执行流程：
1. 证明方随机选取置换 $\sigma=(2\ 4)$（交换顶点2和4的位置），计算 $H=\sigma(G_0)$，得到 $H$ 的边集为 $\{(1,4),(4,3),(3,2),(2,1)\}$，将 $H$ 发送给验证方；
2. 验证方随机生成挑战比特 $b=1$，发送给证明方；
3. 证明方计算响应置换 $\rho=\sigma\circ\pi^{-1}=(1\ 4\ 3\ 2)$，发送给验证方；
4. 验证方计算 $\rho(G_1)$，结果与 $H$ 完全一致，验证通过。
\end{example}

\subsection{完整安全证明}
GMR协议的零知识属性通过\textbf{回溯模拟技术}证明：模拟器通过重置恶意验证方的状态，预先适配挑战比特，在不掌握私密证据的前提下复刻完整真实视图。

\subsubsection{零知识属性证明}
\begin{fullproof}[零知识属性]
构造PPT回溯模拟器 $\Sim$，对任意恶意验证方 $\Adv$，复刻真实交互视图：
\begin{enumerate}
\item \textbf{初始化阶段}：$\Sim$ 获取公开输入图 $G_0,G_1$，以及恶意验证方 $\Adv$ 的随机执行种子 $r$（保证回溯后行为可重现）；
\item \textbf{回溯模拟循环}：
  \begin{enumerate}
  \item $\Sim$ 随机预选一个猜测的挑战比特 $b^*\gets\{0,1\}$，再随机选取一个置换 $\rho^*\gets\mathsf{Perm}(V)$；
  \item 根据预选比特 $b^*$，构造模拟承诺图 $H^*=\rho^*(G_{b^*})$，将 $H^*$ 发送给恶意验证方 $\Adv$；
  \item $\Adv$ 基于种子 $r$ 和承诺 $H^*$，生成实际挑战比特 $b$ 并发送给 $\Sim$；
  \item 若 $b\neq b^*$，说明猜测失败，$\Sim$ 回溯 $\Adv$ 的内部状态到承诺发送前，重置随机种子为 $r$，重新选取 $b^*$ 和 $\rho^*$，重复上述流程；
  \item 若 $b=b^*$，说明猜测成功，$\Sim$ 将预选的置换 $\rho^*$ 作为响应发送给 $\Adv$；
  \end{enumerate}
\item \textbf{视图输出阶段}：$\Sim$ 提取 $\Adv$ 在交互过程中的所有输出（承诺图 $H^*$、挑战比特 $b$、响应置换 $\rho^*$），组合成模拟视图 $\View_\Sim^\Ideal$。
\end{enumerate}

接下来证明真实视图与模拟视图计算不可区分：
- 在真实世界中，证明方的置换 $\sigma$、挑战比特 $b$ 均为均匀随机选取，因此承诺图 $H=\sigma(G_0)$、响应置换 $\rho$ 的分布，与 $b$ 的取值严格无关；
- 在理想世界中，模拟器的预选比特 $b^*$、置换 $\rho^*$ 同样均匀随机，且猜测成功的概率为 $1/2$，回溯过程不会改变随机分布的一致性。

对任意PPT区分器，其能观察到的真实视图与模拟视图的分布完全重合，区分优势严格为0。因此，GMR协议满足基于模拟的零知识属性。
\end{fullproof}

\subsubsection{可靠性证明}
\begin{fullproof}[可靠性]
采用反证法证明：假设存在PPT恶意证明方 $\Adv$，在 $G_0$ 与 $G_1$ 不同构的前提下，以不可忽略概率 $\varepsilon(\kappa)$ 欺骗验证方接受错误命题。

此时可构造PPT算法 $\mathcal{B}$，调用 $\Adv$ 作为黑盒子程序，区分图的同构性：
\begin{enumerate}
\item 挑战者给出两个不同构的图 $G_0,G_1$，发送给算法 $\mathcal{B}$；
\item 算法 $\mathcal{B}$ 与 $\Adv$ 执行协议流程，获取 $\Adv$ 的承诺消息 $H$；
\item 算法 $\mathcal{B}$ 随机选择两个不同的挑战比特 $b_1,b_2\gets\{0,1\}$，分别获取 $\Adv$ 的响应置换 $\rho_1,\rho_2$；
\item 若 $\Adv$ 能正确响应两个挑战，则说明 $H$ 同时与 $G_0,G_1$ 同构，与前提矛盾；若 $\Adv$ 只能响应一个挑战，则算法 $\mathcal{B}$ 可正确判定图的同构性。
\end{enumerate}

单轮协议中，恶意证明方的成功欺骗概率不超过 $1/2$；通过 $n$ 轮独立迭代执行协议，欺骗概率可压缩至 $(1/2)^n$，对应安全参数下的可忽略级别，因此GMR协议满足可靠性。
\end{fullproof}

\section{公钥加密：ElGamal 加密}
ElGamal加密是基于离散对数困难假设的经典公钥加密方案，也是极少数满足同态乘法属性的加密方案，广泛应用于电子投票、数字版权等隐私协议中。

\subsection{系统模型}
\begin{sysmodel}
ElGamal加密方案是标准的三方公钥加密协议，包含三个核心算法：
\begin{enumerate}
\item 密钥生成算法 $\Gen(1^\kappa)\to(pk,sk)$：输入安全参数 $\kappa$，生成公钥 $pk$（公开）和私钥 $sk$（保密留存）；
\item 加密算法 $\Enc_{pk}(m;r)\to c$：输入公钥 $pk$、明文消息 $m$、随机盲化因子 $r$，输出密文 $c$；
\item 解密算法 $\Dec_{sk}(c)\to m/\perp$：输入私钥 $sk$、密文 $c$，输出明文 $m$ 或解密失败符号 $\perp$。
\end{enumerate}
安全目标：保证抗选择明文攻击（IND-CPA）安全，在敌手获取大量密文的前提下，仍无法破解明文的有效信息。
\end{sysmodel}

\subsection{安全模型（基于模拟的IND-CPA安全）}
ElGamal的IND-CPA安全属性通过模拟范式形式化定义，严格对抗拥有加密oracle访问权限的恶意敌手。

\begin{secmodel}[基于模拟的IND-CPA安全]
方案满足IND-CPA安全属性，当且仅当对任意PPT恶意敌手 $\Adv$，存在PPT模拟器 $\Sim$，使得真实世界中 $\Adv$ 执行以下游戏获得的真实视图，与理想世界中 $\Sim$ 生成的模拟视图计算不可区分：
\begin{enumerate}
\item \textbf{初始化阶段}：挑战者运行密钥生成算法 $(pk,sk)\gets\Gen(1^\kappa)$，将公钥 $pk$ 发送给敌手 $\Adv$；
\item \textbf{查询阶段}：$\Adv$ 可任意选择明文消息，调用加密oracle获取对应的密文；
\item \textbf{挑战阶段}：$\Adv$ 输出两个等长挑战明文 $m_0,m_1$，挑战者随机选取比特 $b\gets\{0,1\}$，返回挑战密文 $c=\Enc_{pk}(m_b)$；
\item \textbf{猜测阶段}：$\Adv$ 输出对 $b$ 的猜测结果，若猜测正确则攻击成功。
\end{enumerate}
模拟器 $\Sim$ 不掌握私钥 $sk$，仅依靠公开信息复刻挑战密文视图，保证敌手的猜测优势不超过可忽略级别。
\end{secmodel}

\subsection{方案构造}
ElGamal加密的安全性归约到\textbf{判定性Diffie-Hellman（DDH）困难假设}，其构造逻辑基于循环群上的临时密钥单向协商机制。

\subsubsection{算法构造}
\begin{construction}[ElGamal加密算法]
\begin{itemize}
\item \textbf{密钥生成算法 $\Gen(1^\kappa)$}：
  \begin{enumerate}
  \item 生成大素数 $q$ 阶循环群 $\mathbb{G}$，选取生成元 $g\in\mathbb{G}$；
  \item 随机选取私钥 $sk=x\gets\mathbb{Z}_q^*$；
  \item 计算对应的公钥 $pk=g^x\in\mathbb{G}$；
  \item 公开参数为 $(\mathbb{G},q,g,pk)$，保密私钥 $sk=x$。
  \end{enumerate}
\item \textbf{加密算法 $\Enc_{pk}(m;r)$}：
  输入明文 $m\in\mathbb{G}$、随机临时密钥 $r\gets\mathbb{Z}_q^*$，计算密文对：
  \[
  c=(c_1,c_2)=\left(g^r,\ m\cdot pk^r\right)
  \]
\item \textbf{解密算法 $\Dec_{sk}(c)$}：
  输入密文对 $c=(c_1,c_2)$，使用私钥 $x$ 计算明文：
  \[
  m = c_2 \cdot (c_1^x)^{-1}
  \]
\end{itemize}
\end{construction}

\subsubsection{Concrete数值样例}
用极小安全参数的模运算样例，演示ElGamal加解密流程：
\begin{example}
取安全参数 $\kappa=4$，生成素数阶循环群 $\mathbb{G}=\mathbb{Z}_{23}^*$，阶 $q=11$，生成元 $g=5$；随机选取私钥 $x=7$，计算公钥 $pk=g^x\mod23=5^7\mod23=17$；公开参数为 $(23,11,5,17)$，保密私钥7。

加密方选择明文消息 $m=10$，随机临时密钥 $r=3$，计算密文：
\[
c_1=g^r\mod23=5^3\mod23=10,\quad c_2=m\cdot pk^r\mod23=10\cdot17^3\mod23=10\cdot14\mod23=6
\]
最终密文为 $c=(10,6)$。

解密方使用私钥 $x=7$ 计算明文：
\[
c_1^x\mod23=10^7\mod23=12,\quad m=c_2\cdot(c_1^x)^{-1}\mod23=6\cdot12^{-1}\mod23=6\cdot2\mod23=10
\]
解密结果与原始明文完全一致，加解密流程正确。
\end{example}

\subsection{完整安全证明}
ElGamal的IND-CPA安全属性归约到DDH困难假设：若存在敌手破坏方案安全，则可构造算法破解DDH问题。

\begin{fullproof}[IND-CPA安全证明]
构造PPT模拟器 $\Sim$，对任意PPT敌手 $\Adv$，复刻不可区分的密文视图：
\begin{enumerate}
\item \textbf{初始化阶段}：$\Sim$ 获取DDH挑战实例——三元组 $(g^a,g^b,g^c)\in\mathbb{G}^3$，其中 $a,b,c\gets\mathbb{Z}_q^*$；若 $c=ab$ 则为真实DDH元组，否则为随机元组；
\item $\Sim$ 设置公钥 $pk=g^a$，将公开参数 $(\mathbb{G},q,g,pk)$ 发送给敌手 $\Adv$；
\item \textbf{查询阶段}：$\Adv$ 调用加密oracle查询任意明文的密文，$\Sim$ 使用自己选取的随机因子计算并返回密文；
\item \textbf{挑战阶段}：$\Adv$ 输出两个等长挑战明文 $m_0,m_1$，$\Sim$ 随机选取比特 $b\gets\{0,1\}$，构造模拟挑战密文：
  \[
  c^*=(c_1^*,c_2^*)=\left(g^b,\ m_b\cdot g^c\right)
  \]
  将 $c^*$ 返回给 $\Adv$；
\item \textbf{猜测阶段}：$\Adv$ 输出对比特 $b$ 的猜测结果，$\Sim$ 检查三元组是否满足DDH条件：
  \begin{itemize}
  \item 若 $c=ab$，则模拟密文 $c^*$ 与真实世界的合法密文分布完全一致；
  \item 若 $c\neq ab$，则 $c^*$ 为完全随机的群元素，与明文 $m_b$ 无关；
  \end{itemize}
\item $\Sim$ 输出 $\Adv$ 的所有交互内容，组合成模拟视图 $\View_\Sim^\Ideal$。
\end{enumerate}

分析视图不可区分性：
- 在真实世界中，临时密钥 $r$ 均匀随机，密文 $c=(g^r,m_b\cdot pk^r)$ 由DDH元组 $(g,g^r,pk,pk^r)$ 生成；
- 在理想世界中，模拟器使用DDH挑战元组构造密文，若敌手 $\Adv$ 能区分真实视图与模拟视图，则 $\Sim$ 可直接调用 $\Adv$ 作为子程序，区分DDH元组与随机元组。

根据DDH困难假设，任意PPT区分器的区分优势可忽略，因此真实视图与模拟视图计算不可区分，ElGamal加密满足基于模拟的IND-CPA安全。
\end{fullproof}

\section{密钥交换：Sigma 协议}
Sigma协议是经典的双向显式认证密钥交换协议，融合了临时Diffie-Hellman（DH）密钥协商与数字签名认证机制，其安全性基于DDH困难假设与数字签名不可伪造性，是TLS 1.3、IKE等主流密钥交换协议的底层核心框架。

\subsection{系统模型}
\begin{sysmodel}
Sigma协议包含两个合法参与方：客户端 $A$、服务端 $B$，双方各自拥有长期数字签名密钥对 $(sk_A,pk_A)$、$(sk_B,pk_B)$，且提前获取对方的合法公钥证书。协议目标：在主动敌手控制的公开网络上，协商出一个会话密钥 $K$，保证会话密钥的机密性、完整性，以及协商过程的前向安全性。

协议执行环境：存在PPT主动敌手 $\Adv$，完全控制双方的通信信道，可以篡改、丢弃、重放消息，或伪造合法参与方的身份。

协议执行流程分为四个步骤：
\begin{enumerate}
\item 双方交换临时DH公钥消息；
\item 双方验证对方临时公钥的合法性；
\item 双方使用长期签名密钥认证临时DH协商参数；
\item 双方基于合法的DH共享秘密，计算出一致的会话密钥 $K$。
\end{enumerate}
\end{sysmodel}

\subsection{安全模型（基于模拟的会话密钥安全）}
密钥交换协议的核心安全目标：会话密钥与均匀随机的二进制串计算不可区分，保证敌手无法从协商过程中获取会话密钥的任何有效信息。

\begin{secmodel}[基于模拟的会话密钥安全]
协议满足会话密钥安全，当且仅当对任意PPT主动敌手 $\Adv$，存在PPT模拟器 $\Sim$，使得真实世界中 $\Adv$ 与协议参与方交互获取的\textbf{会话密钥视图}，与理想世界中 $\Sim$ 生成的\textbf{随机密钥模拟视图}计算不可区分。

正式安全判定条件：
\[
\left|\Pr\left[\mathcal{D}(\View_\Adv^\Real,K)=1\right] - \Pr\left[\mathcal{D}(\View_\Sim^\Ideal,R)=1\right]\right| \le \negl(\kappa)
\]
其中 $K$ 是真实协商的会话密钥，$R$ 是同长度的均匀随机串，$\mathcal{D}$ 是任意PPT区分器。

模拟器 $\Sim$ 不掌握任何参与方的长期私钥或临时DH私钥，仅依靠公开信息与数字签名的不可伪造性，复刻完整的密钥协商视图。
\end{secmodel}

\subsection{方案构造}
Sigma协议的完整构造融合了临时DH密钥协商与数字签名认证机制，细节流程如下：

\begin{construction}[Sigma协议完整交互流程]
公共前置条件：参与方 $A,B$ 拥有长期数字签名密钥对 $(sk_A,pk_A)$、$(sk_B,pk_B)$，且互相持有对方的合法公钥证书；公开全局DH参数：素数阶循环群 $\mathbb{G}$、生成元 $g\in\mathbb{G}$。

协议执行步骤：
\begin{enumerate}
\item \textbf{临时DH公钥交换阶段}：
  \begin{enumerate}
  \item $A$ 随机选取临时DH私钥 $a\gets\mathbb{Z}_q^*$，计算临时公钥 $X_A=g^a$，发送给 $B$；
  \item $B$ 随机选取临时DH私钥 $b\gets\mathbb{Z}_q^*$，计算临时公钥 $X_B=g^b$，发送给 $A$；
  \end{enumerate}
\item \textbf{共享秘密计算阶段}：
  \begin{enumerate}
  \item $A$ 验证 $X_B\in\mathbb{G}$ 为合法群元素，计算临时共享秘密 $K_{AB}=X_B^a=g^{ab}$；
  \item $B$ 验证 $X_A\in\mathbb{G}$ 为合法群元素，计算临时共享秘密 $K_{BA}=X_A^b=g^{ab}$；
  \end{enumerate}
\item \textbf{参数认证阶段}：
  \begin{enumerate}
  \item $A$ 使用长期私钥 $sk_A$，对协商参数 $(X_A,X_B)$ 生成数字签名 $\sigma_A=\mathsf{Sign}_{sk_A}(X_A,X_B)$，将 $\sigma_A$ 发送给 $B$；
  \item $B$ 使用长期私钥 $sk_B$，对协商参数 $(X_A,X_B)$ 生成数字签名 $\sigma_B=\mathsf{Sign}_{sk_B}(X_A,X_B)$，将 $\sigma_B$ 发送给 $A$；
  \end{enumerate}
\item \textbf{会话密钥生成阶段}：
  \begin{enumerate}
  \item $A$ 验证签名 $\sigma_B$ 的合法性，若验证通过则计算会话密钥 $K=\mathsf{KDF}(K_{AB})$；
  \item $B$ 验证签名 $\sigma_A$ 的合法性，若验证通过则计算会话密钥 $K=\mathsf{KDF}(K_{BA})$；
  \end{enumerate}
\end{enumerate}
其中 $\mathsf{KDF}(\cdot)$ 是公开的密码学密钥派生函数，将临时DH共享秘密转换为指定长度的可用会话密钥。
\end{construction}

\subsection{完整安全证明}
Sigma协议的安全性归约到两个核心密码学假设：DDH困难假设（保证DH共享秘密的机密性）、数字签名的选择消息攻击下存在性不可伪造性（EUF-CMA，保证协商参数的合法性）。

\begin{fullproof}[基于模拟的会话密钥安全证明]
构造PPT模拟器 $\Sim$，对任意PPT主动敌手 $\Adv$，复刻不可区分的会话密钥视图：
\begin{enumerate}
\item \textbf{初始化阶段}：$\Sim$ 获取DDH挑战实例 $(g^a,g^b,g^c)\in\mathbb{G}^3$，以及参与方 $A,B$ 的长期签名公钥 $pk_A,pk_B$；
\item \textbf{模拟消息交换阶段}：
  \begin{enumerate}
  \item $\Sim$ 代替 $A$ 发送临时公钥 $X_A=g^a$，代替 $B$ 发送临时公钥 $X_B=g^b$；
  \item 若敌手 $\Adv$ 篡改临时公钥，$\Sim$ 检测到非法参数，直接终止协议交互；
  \end{enumerate}
\item \textbf{模拟认证阶段}：
  \begin{enumerate}
  \item $\Sim$ 使用签名模拟器，生成合法的参数签名 $\sigma_A,\sigma_B$，发送给敌手 $\Adv$；
  \item 若 $\Adv$ 伪造合法参与方的签名，根据EUF-CMA不可伪造性，$\Sim$ 以极大概率检测到伪造行为，终止协议；
  \end{enumerate}
\item \textbf{模拟会话密钥阶段}：
  \begin{enumerate}
  \item $\Sim$ 检查DDH挑战元组的合法性：若 $c=ab$，则真实共享秘密为 $g^{ab}$；否则为随机群元素 $g^c$；
  \item $\Sim$ 计算模拟会话密钥 $K^*=\mathsf{KDF}(g^c)$，将 $K^*$ 与所有交互消息组合成模拟视图 $\View_\Sim^\Ideal$；
  \end{enumerate}
\item \textbf{视图输出阶段}：$\Sim$ 将模拟视图返回给区分器 $\mathcal{D}$。
\end{enumerate}

接下来证明视图不可区分性：
- 在真实世界中，临时DH私钥 $a,b$ 均匀随机，共享秘密 $g^{ab}$ 由合法DDH元组生成，会话密钥 $K=\mathsf{KDF}(g^{ab})$ 与协商参数完全绑定；
- 在理想世界中，模拟器使用DDH挑战元组构造会话密钥：若敌手 $\Adv$ 能区分真实会话密钥与模拟随机密钥，则 $\Sim$ 可直接调用 $\Adv$ 作为子程序，区分DDH元组与随机元组，与DDH困难假设矛盾；
- 若 $\Adv$ 尝试篡改协商参数或伪造签名，则根据数字签名的不可伪造性，$\Sim$ 能以极大概率检测到攻击，视图仍保持不可区分性。

因此，对任意PPT主动敌手 $\Adv$，真实世界视图与理想世界模拟视图计算不可区分，Sigma协议满足基于模拟的会话密钥安全。
\end{fullproof}

\section{基于模拟的Hybribs}
Hybrids是基于模拟的安全证明中常用的技术工具，通过构造一系列逐步过渡的“混合世界”，将真实协议执行逐渐转化为理想功能执行，便于分析每一步的安全性变化。
\subsection{Hybrids的构造逻辑}
构造Hybrids的核心逻辑是：在每个混合世界中，
仅修改协议执行的一个特定方面（如替换某个算法、引入模拟器、改变某个消息的生成方式），并证明该修改不会显著增加敌手的区分优势。
通过逐步替换协议组件，最终将真实协议执行完全转化为理想
功能执行，证明整体安全性。
\subsection{Hybrids的分析方法}
分析每个混合世界的安全性变化，通常采用以下方法：
\begin{itemize}
\item \textbf{概率分析}：计算敌手在每个混合世界的攻击成功概率，证明相邻混合世界之间的概率差异可忽略；
\item \textbf{分布重合}：证明相邻混合世界中敌手观察到的消息分布完全重合，区分优势为0；
\item \textbf{归约分析}：将混合世界的修改行为归约到破解底层数学假设或密码学原语的算法行为，证明若敌手能区分混合世界，则可构造算法破解假设，与安全假设矛盾。
\item \textbf{模拟器适配}：在引入模拟器的混合世界中，证明模拟器能够成功复刻真实视图，保持敌手的区分优势不变。
\end{itemize}
\subsection{Canetti-Krawczyk (CK) 密钥协商协议}
Canetti-Krawczyk 协议是密码学中第一个在通用可组合（UC）框架下给出安全证明的认证密钥协商协议之一。它的安全证明不仅依赖于标准的离散对数/DDH 假设和签名不可伪造性，还通过一系列 hybrid 过渡步骤将真实协议执行逐步转化为理想功能执行，是理解“基于模拟+hybrid 证明”技术的经典范例。

\subsubsection{系统模型}
\begin{sysmodel}
CK 协议包含两个通信参与方：
\begin{itemize}
  \item 参与方 $A$ 和 $B$，各自拥有长期签名密钥对 $(\text{sk}_A, \text{vk}_A)$ 和 $(\text{sk}_B, \text{vk}_B)$；
  \item 每次会话内，双方分别生成一次性 DH 私钥 $a,b$ 并计算临时公钥 $X=g^a$、$Y=g^b$；
  \item 协议在不可信网络中执行，敌手 $\Adv$ 可以控制消息的转发、修改、延迟和重放，并可启动并发会话，目标是破坏会话密钥的机密性或完整性。
\end{itemize}
\end{sysmodel}

\subsubsection{安全模型}
CK 协议的安全模型采用基于模拟的真实/理想世界定义。为便于后续安全证明，我们首先给出真实世界的形式化描述。

\paragraph{真实世界定义}
真实世界执行由协议 $\Pi_{\text{CK}}$ 与真实世界敌手 $\Adv$ 共同完成。该世界包括：
\begin{itemize}
  \item 诚实参与方 $A$ 和 $B$，各自持有长期密钥对 $(\text{sk}_A,\text{vk}_A)$ 和 $(\text{sk}_B,\text{vk}_B)$；
  \item 每次会话生成独立的一次性 DH 私钥 $a,b$，并计算临时公钥 $X=g^a$、$Y=g^b$；
  \item 敌手 $\Adv$ 完全控制通信网络，能够拦截、修改、转发、延迟和重放任意协议消息；
  \item 敌手可发起并发会话，选择会话参与方、会话标识和消息调度顺序；
  \item 敌手可对某些会话查询状态信息，包括但不限于：
  \begin{itemize}
    \item \textbf{Corrupt 查询}：获取某个参与方的长期私钥；
    \item \textbf{Reveal 查询}：获取某个会话的会话密钥；
    \item \textbf{Session-State 查询}：获取某个进行中会话的临时会话状态。
  \end{itemize}
\end{itemize}

真实世界实验 $\text{Real}^{\Pi_{\text{CK}},\Adv}(\kappa)$ 的输出是环境 $\mathcal{Z}$ 对整个执行过程的判别结果。该实验的核心目标是：对任何未被暴露的诚实会话，其会话密钥必须对敌手计算不可区分。

\paragraph{理想世界定义}
理想世界由理想功能 $\mathcal{F}_{\text{AKE}}$ 与模拟器 $\Sim$ 共同完成。理想功能严格定义认证密钥交换的安全语义：
\begin{itemize}
  \item 当双方发起一条未被腐化的会话时，$\mathcal{F}_{\text{AKE}}$ 生成一个均匀随机的会话密钥 $K$，并将该密钥交付给会话双方；
  \item 对于被腐化或已暴露的会话，$\mathcal{F}_{\text{AKE}}$ 按模型规定泄露相应的状态信息给敌手；
  \item $\mathcal{F}_{\text{AKE}}$ 不向敌手泄露未暴露会话的密钥。
\end{itemize}

理想世界实验 $\text{Ideal}^{\mathcal{F}_{\text{AKE}},\Sim}(\kappa)$ 的执行过程如下：
\begin{enumerate}
  \item 模拟器 $\Sim$ 接收来自敌手 $\Adv$ 的所有网络控制请求和乱序消息，并根据其对理想功能的查询生成模拟视图；
  \item 若敌手发起腐化或会话查询，$\Sim$ 将相应信息传递给 $\mathcal{F}_{\text{AKE}}$；
  \item $\mathcal{F}_{\text{AKE}}$ 返回符合理想安全语义的响应；
  \item $\Sim$ 使用这些响应构造并返回给敌手一个与真实世界视图不可区分的模拟视图。
\end{enumerate}

\paragraph{安全性定义}
CK 协议在基于模拟的安全模型下满足安全性，当且仅当对任意多项式时间敌手 $\Adv$，存在多项式时间模拟器 $\Sim$，使得对任意多项式时间环境 $\mathcal{Z}$，真实世界实验与理想世界实验的输出分布计算不可区分：
\[ \left|\Pr\left[\mathcal{Z}\left(\text{Real}^{\Pi_{\text{CK}},\Adv}(\kappa)\right)=1\right] - \Pr\left[\mathcal{Z}\left(\text{Ideal}^{\mathcal{F}_{\text{AKE}},\Sim}(\kappa)\right)=1\right] \right| \leq \negl(\kappa). \]

这一形式化定义与后续安全证明紧密对应：真实世界定义明确了敌手的网络控制能力、会话并发与暴露查询；理想世界定义则明确了模拟器只可从理想功能获取信息，不得直接访问诚实方的私钥。这样的定义为构造 hybrid 步骤和模拟器归约提供了清晰的逻辑基础。

\subsubsection{方案构造}
CK 协议的核心构造是对经典 Diffie-Hellman 密钥交换加上签名认证与会话标识。
\begin{construction}[CK 协议]
协议参数：生成元 $g$ 和大素数阶循环群 $\mathbb{G}$，以及一个密钥派生函数 $\mathsf{KDF}$。

双方前置持有长期签名密钥对 $(\text{sk}_A,\text{vk}_A)$ 和 $(\text{sk}_B,\text{vk}_B)$。

执行流程如下：
\begin{enumerate}
  \item 参与方 $A$ 生成临时 DH 私钥 $a\gets\mathbb{Z}_q$，计算 $X=g^a$，并发送消息 $m_A = (\mathsf{ID}_A,\mathsf{ID}_B,X,\sigma_A)$ 给 $B$，其中 $\sigma_A=\textsf{Sign}_{\text{sk}_A}(\mathsf{ID}_A,\mathsf{ID}_B,X)$；
  \item 参与方 $B$ 生成临时 DH 私钥 $b\gets\mathbb{Z}_q$，计算 $Y=g^b$，验证 $\sigma_A$ 的有效性；若验证通过，计算 $m_B = (\mathsf{ID}_B,\mathsf{ID}_A,Y,\sigma_B)$ 并发送给 $A$，其中 $\sigma_B=\textsf{Sign}_{\text{sk}_B}(\mathsf{ID}_B,\mathsf{ID}_A,Y)$；
  \item 双方验证对方签名合法性，并分别计算共享 DH 值 $Z = X^b = Y^a = g^{ab}$；
  \item 最终会话密钥为 $K = \mathsf{KDF}(\mathsf{ID}_A,\mathsf{ID}_B,X,Y,Z)$。
\end{enumerate}
\end{construction}

该构造通过签名绑定会话标识与临时 DH 值，防止中间人攻击和会话重放，并使得会话密钥与双方身份唯一绑定。

\subsubsection{安全性证明}
CK 协议的安全性证明通过构造一系列混合世界（hybrid）逐步将真实协议执行转化为理想功能执行，并证明每一步的改动对敌手的观察不可区分。

我们定义以下混合世界：
\begin{itemize}
  \item \textbf{Hybrid H0：真实世界。} 诚实参与方按 CK 协议真实执行，消息由双方使用各自长期签名密钥进行签名，未暴露会话的共享值为真实 DH 共享 $g^{ab}$，最终密钥由 $\mathsf{KDF}(\mathsf{ID}_A,\mathsf{ID}_B,X,Y,g^{ab})$ 生成。
  \item \textbf{Hybrid H1：签名模拟。} 在 H0 的基础上，模拟器替换诚实参与方的签名过程，让所有来自诚实一方的签名在敌手未知的情况下，由模拟器代签或由签名挑战接口生成。该世界与 H0 的差异仅在于签名源是否为真实私钥直签。
  \item \textbf{Hybrid H2：DH 随机化。} 在 H1 的基础上，对于所有仍未暴露的会话，将真实的 DH 共享值 $g^{ab}$ 替换为一个独立均匀随机群元素 $R\in\mathbb{G}$。其他协议步骤保持不变。
  \item \textbf{Hybrid H3：会话密钥随机化。} 在 H2 的基础上，将未暴露会话的最终会话密钥 $K$ 直接替换为一个均匀随机值 $K^*\in\{0,1\}^\lambda$，而不再通过 $\mathsf{KDF}$ 计算。此时敌手对该会话的所有可见信息已经与真实协议执行分离。
  \item \textbf{Hybrid H4：理想世界。} 模拟器完全遵循理想功能 $\mathcal{F}_{\text{AKE}}$，使用理想功能返回会话密钥，并根据敌手的查询生成相应的消息视图。
\end{itemize}

下面逐步说明每个相邻混合世界之间的不可区分性。

\paragraph{H0 与 H1 的不可区分性}
两个混合世界的唯一区别是诚实参与方签名的生成方式。若存在 PPT 敌手能够区分 H0 与 H1，则可以构造一个签名伪造算法 $\\mathcal{B}_{\text{SIG}}$：
\begin{itemize}
  \item $\\mathcal{B}_{\text{SIG}}$ 将自身的签名挑战接口作为模拟器的签名 oracle，模拟 H1 中诚实参与方的签名；
  \item 当敌手试图区分时，若它能察觉签名来源差异，则必然需要伪造一个未见过的签名查询结果；
  \item $\\mathcal{B}_{\text{SIG}}$ 使用该伪造签名作为自己的挑战回应，从而直接破解 EUF-CMA 签名不可伪造性。
\end{itemize}
因此，在签名方案满足 EUF-CMA 安全的前提下，H0 与 H1 的区分优势为可忽略。

\paragraph{H1 与 H2 的不可区分性}
H1 与 H2 的区别在于未暴露会话中用于计算会话密钥的共享值是否是真实 DH 共享值 $g^{ab}$，还是独立随机群元素 $R$。若敌手能区分 H1 与 H2，则可以构造一个 DDH 区分器 $\\mathcal{B}_{\text{DDH}}$：
\begin{itemize}
  \item $\\mathcal{B}_{\text{DDH}}$ 从 DDH 挑战中得到一个元组 $(g^a,g^b,T)$，并将其嵌入某个未暴露会话的临时公钥与共享值；
  \item 若 $T=g^{ab}$，则该会话对应 H1 的真实 DH 共享值；若 $T$ 是随机值，则该会话对应 H2 中的随机共享值；
  \item $\\mathcal{B}_{\text{DDH}}$ 将该混合世界交给敌手，若敌手能区分它返回的视图，则 $\\mathcal{B}_{\text{DDH}}$ 可判断 DDH 元组性质。
\end{itemize}
因此，在 DDH 假设成立时，H1 与 H2 的区分优势也为可忽略。

\paragraph{H2 与 H3 的不可区分性}
H2 与 H3 的区别仅在于会话密钥是否由 KDF 从随机共享值派生，还是直接使用均匀随机密钥。若 KDF 满足伪随机函数或随机提取器性质，则由随机群元素派生的会话密钥与独立均匀随机密钥在敌手视图下计算不可区分。换言之，
\begin{itemize}
  \item 若敌手能区分 H2 与 H3，则可以构造一个针对 KDF 的区分器，违背 KDF 的安全假设；
  \item 因此在安全的 KDF 假设下，H2 与 H3 之间的区别对敌手不可察觉。
\end{itemize}
如果希望更为严格，本文也可以将 H3 定义为“模拟器直接从理想功能获取密钥”，这样 H3 与理想世界 H4 的差异只在于模拟器是否直接调用理想功能。

\paragraph{H3 与 H4 的不可区分性}
在 H3 中，未暴露会话的会话密钥已被替换为独立随机值，而模拟器仍然以与真实协议相同的方式向敌手生成消息视图。此时只要模拟器能够正确处理已暴露会话和被腐化参与方的状态泄露，H3 与 H4 在敌手可见视图上的差异就仅剩理想功能语义上的等价性。由于 H3 已经消除了真实 DH 共享的依赖，模拟器可以直接从理想功能获取相同的会话密钥，因此两者实际上是完全等价的。

\paragraph{总结性归约}
上述 hybrid 过渡形成一条链条：
\[ H0 \approx H1 \approx H2 \approx H3 \approx H4. \]
其中每个“\(\approx\)”都意味着相邻世界之间的区分优势可忽略。综合这条链条，真实世界视图与理想世界视图的总区分优势也是可忽略的，从而证明了 CK 协议在基于模拟的 UC AKE 安全模型下是安全的。

这个证明过程的关键点是：
\begin{itemize}
  \item 仅允许在单一步骤中改变协议执行的一项核心元素；
  \item 每一步的不可区分性由一个明确的安全假设支撑；
  \item 模拟器通过签名 oracle、DDH 挑战和理想功能查询，保持对敌手视图的精确控制。
\end{itemize}

这样，CK 协议的安全性证明就变成了一个清晰的 hybrid 归约链，每个环节都有明确的假设和不可区分性理由。

\section{总结}
本讲义深入剖析了四个经典密码学原语的基于模拟的安全证明逻辑，所有方案的安全规约逻辑均遵循“系统模型定义-安全模型形式化-方案构造实现-困难假设归约”的标准科研范式。

\subsection{经典方案的模拟逻辑本质}
通过四个方案的完整证明，可以总结出基于模拟的安全证明的核心通用逻辑：
\begin{enumerate}
\item \textbf{明确双世界边界}：真实世界中敌手拥有完整网络控制权；理想中无恶意敌手，仅由模拟器依靠公开信息复刻攻击视图；
\item \textbf{定制模拟器策略}：根据协议原语类型，选择合适的模拟器——承诺协议采用标准黑盒模拟器，零知识协议采用回溯模拟器，密钥交换协议采用签名包装模拟器；
\item \textbf{归约到困难假设}：将“区分真实/模拟视图”的敌手攻击行为，转化为破解底层数学困难假设（离散对数、DDH）或密码学原语假设（签名不可伪造性）的算法行为；
\item \textbf{证明分布不可区分}：通过随机变量分布重合、概率优势可忽略，严谨论证敌手无法分辨真实协议执行与模拟器的伪造执行。
\end{enumerate}

\subsection{模拟范式的核心优势}
相比基于游戏的安全证明范式，基于模拟的范式更适合刻画复杂交互协议的全局安全：
\begin{itemize}
\item \textbf{安全属性完备}：一次性覆盖机密性、完整性、身份认证、前向安全性等所有安全维度，无需单独定义单个游戏；
\item \textbf{组合兼容性强}：在通用可组合（UC）安全框架下，满足模拟安全的协议可直接与其他安全协议任意组合，仍保持整体安全强度；
\item \textbf{语义清晰直观}：通过“真实攻击可被理想模拟复刻”的逻辑，将抽象的安全属性转化为可验证的视图分布差异，科研证明逻辑更严谨。
\end{itemize}

\bibliographystyle{unsrt}
\bibliography{refs}


\end{document}